---
phase: 04-multi-tab-editor-core
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java
autonomous: false
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-09
tags:
  - javafx
  - wiring
  - integration
must_haves:
  truths:
    - "MainController owns a single EditorController instance, initialised inside MainController.initialize() (FXML lifecycle)"
    - "EditorController receives (editorPane StackPane, primaryStage supplier, MainController::setDirty) at init time"
    - "FileTreeController.setOnFileOpenRequest is wired to editorController::openOrFocusTab — Phase 3 D-05 seam activated"
    - "Double-clicking a file in the FileTree opens it as a new tab in the editor (TREE-04 + EDIT-01 end-to-end)"
    - "Window close with a dirty tab triggers MainController's existing close-request confirmation dialog (Pitfall 4 closed)"
    - "The Phase 1 placeholder Label in editorPane is no longer visible once a tab is opened (Pitfall 5)"
  artifacts:
    - path: src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java
      provides: "Integration wiring: EditorController instantiated, initialised, and connected to the FileTreeController open-seam"
      contains: "private final EditorController editorController"
  key_links:
    - from: src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java
      to: src/main/java/ch/ti/gagi/xsleditor/ui/EditorController.java
      via: "editorController.initialize(editorPane, () -> primaryStage, this::setDirty)"
      pattern: "editorController\\.initialize\\("
    - from: src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java
      to: src/main/java/ch/ti/gagi/xsleditor/ui/FileTreeController.java
      via: "fileTreeController.setOnFileOpenRequest(editorController::openOrFocusTab)"
      pattern: "setOnFileOpenRequest\\(editorController::openOrFocusTab\\)"
---

<objective>
Wire `EditorController` into `MainController` and connect the Phase 3 file-open seam so the
full Phase 4 experience becomes reachable from the UI. This is the smallest possible
integration plan: one field, one `initialize(...)` call, one seam hookup — no new behaviour.

Purpose: Plans 01 and 02 produced the primitives; this plan activates them. It is also the
natural place for the human-verify checkpoint because all EDIT-0x behaviours need live UI
interaction (keyboard focus, modal dialogs, tab rendering) that cannot be automated with the
current JUnit-only test stack.

Output: `MainController.java` with the editor sub-controller initialised and the
`FileTreeController` open-seam connected. No FXML changes. No changes to `EditorController`
or `EditorTab`.
</objective>

<execution_context>
@~/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@~/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-multi-tab-editor-core/04-RESEARCH.md
@.planning/phases/04-multi-tab-editor-core/04-PATTERNS.md
@.planning/phases/04-multi-tab-editor-core/04-02-PLAN.md
@src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java
@src/main/java/ch/ti/gagi/xsleditor/ui/FileTreeController.java
@src/main/java/ch/ti/gagi/xsleditor/ui/EditorController.java

<interfaces>
<!-- What Plan 02 exposed for this plan to call. -->

From src/main/java/ch/ti/gagi/xsleditor/ui/EditorController.java:
```java
public final class EditorController {
    public void initialize(
        StackPane editorPane,
        Supplier<Stage> primaryStageSupplier,
        Consumer<Boolean> dirtyCallback
    );
    public void openOrFocusTab(Path path);
}
```

From src/main/java/ch/ti/gagi/xsleditor/ui/FileTreeController.java (line 117, Phase 3 D-05 seam):
```java
public void setOnFileOpenRequest(Consumer<Path> callback);
// Phase 3 left the default as a no-op. Plan 03 replaces it with editorController::openOrFocusTab.
```

From src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java (existing state — DO NOT recreate):
```java
public class MainController {
    @FXML private StackPane editorPane;                       // Phase 1 fx:id — present
    private Stage primaryStage;                                // populated by setPrimaryStage()
    private final FileTreeController fileTreeController = new FileTreeController();  // line 62
    public void setDirty(boolean dirty);                       // Phase 1 scaffold — line 113
    @FXML public void initialize();                            // line 66 — wiring goes here
}
```

Integration rules (non-negotiable):
- `EditorController` is instantiated as a `private final` field, next to the existing `fileTreeController` field.
- `editorController.initialize(...)` must be called AFTER `fileTreeController.initialize(...)` (the seam it wires into exists only after FileTreeController finishes its own init).
- The `primaryStage` supplier uses `() -> primaryStage` (not `primaryStage` directly) because `MainController.primaryStage` is populated AFTER FXML `initialize()` runs — same deferred pattern `FileTreeController` uses.
- The dirty callback is `this::setDirty` — the existing Phase 1 scaffold. No new method needed on MainController.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire EditorController into MainController and connect the FileTree open-seam</name>
  <files>src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java</files>
  <read_first>
    - src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java (current state — field list at lines 36-62, initialize at 66-82, setDirty at 113-115, setPrimaryStage at 88-91)
    - src/main/java/ch/ti/gagi/xsleditor/ui/FileTreeController.java (line 117 — setOnFileOpenRequest seam contract)
    - src/main/java/ch/ti/gagi/xsleditor/ui/EditorController.java (initialize signature from Plan 02)
    - .planning/phases/04-multi-tab-editor-core/04-PATTERNS.md (MainController modification template, lines 263-292)
  </read_first>
  <behavior>
    - After this task: `new EditorController()` field exists on MainController.
    - After this task: `MainController.initialize()` calls `editorController.initialize(editorPane, () -> primaryStage, this::setDirty)` exactly once, AFTER `fileTreeController.initialize(...)`.
    - After this task: `MainController.initialize()` calls `fileTreeController.setOnFileOpenRequest(editorController::openOrFocusTab)` exactly once, AFTER both initialize() calls.
    - The application compiles and launches without errors.
  </behavior>
  <action>
    Modify `src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` with THREE precise edits. Do not touch any other code.

    Edit 1 — Add the EditorController field directly below the existing `fileTreeController` field (line 62):

    Before (line 62):
    ```
    private final FileTreeController fileTreeController = new FileTreeController();
    ```

    After (two lines):
    ```
    private final FileTreeController fileTreeController = new FileTreeController();
    private final EditorController editorController = new EditorController();  // Phase 4
    ```

    Edit 2 — Inside `MainController.initialize()` (lines 66-82), AFTER the existing `fileTreeController.initialize(...)` block (which ends at line 79) and BEFORE the `statusLabel.setText("")` line (line 81), insert these exact lines:

    ```
            // Phase 4 — EditorController setup (EDIT-01..03, EDIT-09)
            editorController.initialize(
                editorPane,
                () -> primaryStage,
                this::setDirty
            );
            // Wire Phase 3 integration seam (FileTreeController.java line 117, D-05)
            fileTreeController.setOnFileOpenRequest(editorController::openOrFocusTab);
    ```

    Edit 3 — No import changes required: `EditorController` lives in the same package `ch.ti.gagi.xsleditor.ui` as `MainController`.

    DO NOT:
    - Modify `setDirty(boolean)` — Phase 1 scaffold is already correct.
    - Modify `handleCloseRequest(...)` — aggregate dirty is now driven by EditorController via setDirty, so the existing dialog logic works as-is.
    - Add a new `@FXML` field or FXML change — the `editorPane` fx:id already exists (line 42).
    - Instantiate a `TabPane` here — that is EditorController's responsibility.
    - Register a scene-level `Ctrl+S` handler here — that is EditorController's responsibility (per-CodeArea InputMap).
    - Introduce a Phase 5 feature (syntax highlighting, go-to-definition) — out of scope.
  </action>
  <verify>
    <automated>./gradlew build -x test &amp;&amp; ./gradlew test &amp;&amp; ./gradlew shadowJar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private final EditorController editorController = new EditorController();" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `1`.
    - `grep -c "editorController.initialize(" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `1`.
    - `grep -c "fileTreeController.setOnFileOpenRequest(editorController::openOrFocusTab)" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `1`.
    - `grep -c "this::setDirty" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `1`.
    - `grep -cE "\(\) -> primaryStage" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns at least `2` (one for fileTreeController, one for editorController).
    - Multiline grep check (ordering): `editorController.initialize` appears AFTER `fileTreeController.initialize`. Regex: `fileTreeController\.initialize[\s\S]*?editorController\.initialize[\s\S]*?fileTreeController\.setOnFileOpenRequest`.
    - `grep -c "import ch.ti.gagi.xsleditor.ui.EditorController" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `0` — same package, no import needed.
    - `grep -c "new TabPane" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `0` — TabPane is EditorController's concern.
    - `grep -c "setOnKeyPressed" src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java` returns `0` — no scene-level Ctrl+S handler leaked in.
    - `./gradlew build -x test` exits 0.
    - `./gradlew test` exits 0 (Plan 01 EditorTabTest + Phase 1/2/3 tests still green).
    - `./gradlew shadowJar` exits 0 — fat JAR packages cleanly.
  </acceptance_criteria>
  <done>
    MainController wired to EditorController; file-open seam active; application builds and packages; automated tests green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — end-to-end multi-tab editor behaviours (EDIT-01, EDIT-02, EDIT-03, EDIT-09)</name>
  <what-built>
    Complete multi-tab editor experience is now reachable via the UI:
    - Phase 1 `editorPane` Label is replaced by an EditorController-owned TabPane on first file open.
    - Double-click (or Enter) on a file in the FileTree opens it as a tab.
    - Opening the same file twice focuses the existing tab (no duplicates).
    - Tab title shows `*filename` when dirty, `filename` when clean.
    - Ctrl+S on a focused CodeArea saves the file and clears the dirty marker.
    - Closing a dirty tab shows a YES/CANCEL confirmation; CANCEL keeps the tab.
    - Closing the window with any dirty tab triggers the existing Phase 1 aggregate close-confirmation dialog.
  </what-built>
  <files>(none modified — verification runs the built application)</files>
  <action>
    Launch the application via `./gradlew run` and execute the six scenarios in <how-to-verify> below. Each scenario covers a specific requirement (EDIT-01..03, EDIT-09) plus a pitfall regression check. Do NOT modify any source files — this task is verification-only. If a scenario fails, STOP and report the observed behaviour + any terminal stacktrace; the executor must re-plan via gap-closure rather than patch ad-hoc.
  </action>
  <verify>
    <automated>./gradlew run &amp; echo "launch the app, then execute the 6 manual scenarios in how-to-verify"</automated>
  </verify>
  <done>
    All six scenarios behave as described; human verifier types "approved".
  </done>
  <how-to-verify>
    Run the application: `./gradlew run` (JavaFX launcher).

    Use the sample project at `src/test/resources/fixtures/` if available, OR create a temporary project directory with 2-3 `.xsl` files for testing. Menu: File → Open Project → pick the directory.

    1. **EDIT-01 multi-tab + dedup (2 min):**
       a. Double-click file `A.xsl` in the tree. EXPECT: new tab opens showing the file's content.
       b. Double-click file `B.xsl` in the tree. EXPECT: a second tab opens, becomes active.
       c. Double-click `A.xsl` again. EXPECT: focus jumps to the existing `A.xsl` tab; NO third tab appears. TabPane must show exactly 2 tabs.
       d. Visually confirm the Phase 1 "Editor" placeholder Label is NOT visible behind the tabs (Pitfall 5 check).

    2. **EDIT-02 dirty indicator (1 min):**
       a. With `A.xsl` focused and clean, type any character. EXPECT: tab title immediately changes from `A.xsl` to `*A.xsl`.
       b. Press Ctrl+Z (undo) until you are back at the original content. EXPECT: the `*` prefix disappears automatically (this is the `UndoManager.atMarkedPosition` payoff).

    3. **EDIT-03 Ctrl+S save (1 min):**
       a. With `A.xsl` focused, make an edit. EXPECT: tab title becomes `*A.xsl`.
       b. Press Ctrl+S. EXPECT: tab title drops the `*`; becomes `A.xsl`.
       c. Open the file on disk in an external editor (or `cat` it in a shell). EXPECT: the buffer content is persisted.
       d. Make another edit and press Ctrl+S again. Confirm the file on disk updates.

    4. **EDIT-09 close-tab confirmation (2 min):**
       a. Make an edit in `A.xsl` so the tab is dirty (`*A.xsl`).
       b. Click the X on the `A.xsl` tab. EXPECT: a CONFIRMATION dialog appears with header "Close \"A.xsl\" without saving?" and buttons YES / CANCEL.
       c. Click CANCEL. EXPECT: dialog closes, tab STAYS OPEN, edits preserved, title still `*A.xsl`.
       d. Click X again. This time click YES. EXPECT: tab closes, changes discarded.
       e. Close the window dialog variant: make an edit in `B.xsl`, then press window X. EXPECT: the Phase 1 aggregate close-request dialog fires (because `setDirty(true)` was propagated by EditorController). This proves Pitfall 4 is closed.

    5. **Reopen-after-close regression check (Pitfall 3) (1 min):**
       a. Open `A.xsl`, close the tab cleanly (no edits, or YES on the dialog).
       b. Double-click `A.xsl` in the tree again. EXPECT: a new tab opens immediately. If nothing happens or focus lands wrong, the registry was not cleaned up (bug in `setOnClosed`).

    6. **Error surfacing (bonus, 1 min):**
       a. Make an edit in `A.xsl`. Delete the file from disk via a shell (`rm`).
       b. Press Ctrl+S. EXPECT: `Save Failed` error alert appears (file parent missing). Tab remains dirty.

    Total expected time: ~8 minutes.
  </how-to-verify>
  <acceptance_criteria>
    - All six checks above behave as described.
    - Tab titles update reactively (no lag).
    - No uncaught exceptions visible in the terminal running `./gradlew run` during any of the 6 scenarios.
    - No ghost "Editor" placeholder text visible at any point.
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all six scenarios behave as described, or paste the observed behaviour plus any terminal stacktrace for the failing scenario.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainController ↔ EditorController | In-process call; same JVM, same thread (JavaFX Application Thread). No serialisation boundary. |
| FileTreeController → EditorController (via seam) | Paths flow from tree selection (already validated against project root in Phase 3 via `validateProjectRelativePath`) into `openOrFocusTab`. Defence-in-depth: EditorController also normalises on its side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Elevation of Privilege | A malicious `FileTreeController` replacement could inject arbitrary paths into `openOrFocusTab` | accept | Local single-user desktop tool per PRD; no plugin system, no network; attacker with JVM access has already won. Not mitigated. |
| T-04-12 | Tampering | Wiring order bug: `setOnFileOpenRequest` called BEFORE `editorController.initialize(...)` would pass an uninitialised reference | mitigate | Task 1 mandates exact ordering (fileTreeController.initialize → editorController.initialize → setOnFileOpenRequest), with a multiline grep check for the sequence. |
| T-04-13 | Denial of Service | Aggregate `setDirty` not propagated causes silent data loss on window close | mitigate | EditorController (Plan 02) calls `dirtyCallback.accept(...)` on every dirty transition; MainController uses existing `handleCloseRequest` path that reads `dirty` flag. Human-verify step 4e confirms end-to-end. |
</threat_model>

<verification>
- `./gradlew build` exits 0 (compiles + tests + packaging).
- `./gradlew test` shows `ProjectContextTest`, `ProjectConfigTest`, `EditorTabTest` all green.
- `./gradlew shadowJar` produces `build/libs/XSLEditor-1.0.0.jar` successfully.
- Manual checklist in Task 2 signed off by human verifier.
- No Phase 5 features introduced (no syntax highlighting, no autocomplete, no go-to-definition).
</verification>

<success_criteria>
- EDIT-01 live: multi-tab + dedup verified by human.
- EDIT-02 live: `*` prefix reactive on dirty transitions.
- EDIT-03 live: Ctrl+S persists to disk and clears dirty state.
- EDIT-09 live: YES/CANCEL confirmation on closing dirty tab; CANCEL cancels.
- Pitfall 3 closed: tab can be closed and reopened without registry staleness.
- Pitfall 4 closed: window-close dialog fires when any tab is dirty.
- Pitfall 5 closed: no ghost placeholder Label.
- Full Gradle build + packaging green.
- Phase 4 READY for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-tab-editor-core/04-03-SUMMARY.md` covering:
- Exact edits applied to `MainController.java` (with diffs).
- Build + test + shadowJar results.
- Human-verification checklist with status per scenario (approved / issue).
- Final Phase 4 requirement coverage table (EDIT-01..03 + EDIT-09 → file lines).
- Handoff note for Phase 5: "EditorController is in place. Phase 5 extends CodeArea with syntax highlighting via StyleSpans — no EditorController API change required; add a styleClass listener per tab on construction inside buildTab, gated by file extension."
</output>
</content>
</invoke>