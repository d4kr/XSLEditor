---
phase: 14-version-icon-housekeeping
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java
  - src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java
autonomous: true
requirements:
  - VER-02
  - ICON-01

must_haves:
  truths:
    - "App window shows the icon in the macOS title bar (wired before primaryStage.show())"
    - "About dialog shows a 64x64 icon ImageView as the first element in the VBox"
    - "Both icon loads are fail-silent — app and dialog do not crash if icon.png is absent"
  artifacts:
    - path: "src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java"
      provides: "Stage icon wiring before primaryStage.show()"
      contains: "primaryStage.getIcons().add"
    - path: "src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java"
      provides: "ImageView prepended to VBox content in About dialog"
      contains: "ImageView"
  key_links:
    - from: "XSLEditorApp.start()"
      to: "src/main/resources/ch/ti/gagi/xsleditor/icon.png"
      via: "getClass().getResourceAsStream(\"/ch/ti/gagi/xsleditor/icon.png\")"
      pattern: "primaryStage.getIcons\\(\\).add"
    - from: "AboutDialog constructor"
      to: "src/main/resources/ch/ti/gagi/xsleditor/icon.png"
      via: "getClass().getResourceAsStream(\"/ch/ti/gagi/xsleditor/icon.png\")"
      pattern: "ImageView.*iconView"
---

<objective>
Wire the app icon into the JavaFX Stage (window title bar / macOS Dock) and add the icon as an ImageView in the About dialog.

Purpose: Makes the icon visible at two locations — the OS window title bar (ICON-01) and the About dialog (VER-02). Both load paths are fail-silent: if the icon resource is missing, the app launches and the dialog opens without a crash.

Output: `XSLEditorApp.java` with icon wiring before `primaryStage.show()`; `AboutDialog.java` with `ImageView iconView` as first element in the `VBox content`.
</objective>

<execution_context>
@~/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@~/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@~/Developer/XSLEditor/.planning/ROADMAP.md
@~/Developer/XSLEditor/.planning/phases/14-version-icon-housekeeping/14-CONTEXT.md
@~/Developer/XSLEditor/.planning/phases/14-version-icon-housekeeping/14-PATTERNS.md
@~/Developer/XSLEditor/.planning/phases/14-version-icon-housekeeping/14-UI-SPEC.md
@~/Developer/XSLEditor/.planning/phases/14-version-icon-housekeeping/14-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts extracted from codebase. No exploration needed. -->

From XSLEditorApp.java (current state at start of task):

  Imports (lines 1-10):
    import ch.ti.gagi.xsleditor.ui.MainController;
    import javafx.application.Application;
    import javafx.fxml.FXMLLoader;
    import javafx.scene.Scene;
    import javafx.stage.Stage;
    import java.io.IOException;
    import java.net.URL;

  start() method body (lines 32-50) — insertion point is between line 48 and line 49:
    primaryStage.setTitle(APP_NAME);           // line 44
    primaryStage.setMinWidth(900);             // line 45
    primaryStage.setMinHeight(600);            // line 46
    primaryStage.setScene(scene);              // line 47
    hostServicesInstance = getHostServices();  // line 48
    // <<< INSERT ICON BLOCK HERE >>>
    primaryStage.show();                       // line 49

From AboutDialog.java (current state at start of task):

  Imports (lines 1-22):
    import javafx.geometry.Pos; // already imported — no new import needed for Pos.CENTER
    import java.io.InputStream;  // already imported

  VBox assembly (lines 114-117) — full replacement needed:
    // 10. Assemble root VBox
    VBox content = new VBox(8,
        titleLabel, sep1, stackHeader, grid,
        sep2, authorLabel, licenseBox);

  After VBox (lines 119-121) — these lines stay unchanged:
    // 11. Wire DialogPane
    getDialogPane().setContent(content);
    getDialogPane().getButtonTypes().add(ButtonType.CLOSE);

Classpath path for icon: /ch/ti/gagi/xsleditor/icon.png
(icon.png was moved to this path in Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire app icon into XSLEditorApp.start() before primaryStage.show()</name>
  <files>src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java</files>
  <read_first>
    - src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java — read in full to see current imports and exact line numbers of start() method before editing
  </read_first>
  <action>
    Two changes to XSLEditorApp.java:

    CHANGE 1 — Add three imports after the existing import block (after `import java.net.URL;`):

      import javafx.scene.image.Image;
      import java.io.InputStream;
      import java.util.logging.Logger;

    Note: `java.io.IOException` is already imported; `java.io.InputStream` is a separate import.
    Do not duplicate any existing import.

    CHANGE 2 — Insert the icon wiring block between `hostServicesInstance = getHostServices();`
    and `primaryStage.show();` in the start() method:

      // Load and set the application window icon (D-03)
      try (InputStream iconStream =
              getClass().getResourceAsStream("/ch/ti/gagi/xsleditor/icon.png")) {
          if (iconStream != null) {
              Image icon = new Image(iconStream);
              if (!icon.isError()) {
                  primaryStage.getIcons().add(icon);
              } else {
                  Logger.getLogger(XSLEditorApp.class.getName())
                      .warning("App icon image reported an error during loading");
              }
          } else {
              Logger.getLogger(XSLEditorApp.class.getName())
                  .warning("App icon not found on classpath: /ch/ti/gagi/xsleditor/icon.png");
          }
      } catch (Exception e) {
          // Non-fatal: app launches without icon
      }

    The try-with-resources block MUST appear before the existing `primaryStage.show();` line.
    Do not remove or reorder any existing lines in start().
  </action>
  <verify>
    <automated>grep -n "primaryStage.getIcons().add" ~/Developer/XSLEditor/src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep "primaryStage.getIcons().add" XSLEditorApp.java` returns at least one match
    - `grep "import javafx.scene.image.Image;" XSLEditorApp.java` returns one match
    - `grep "import java.io.InputStream;" XSLEditorApp.java` returns one match
    - `grep "import java.util.logging.Logger;" XSLEditorApp.java` returns one match
    - `grep "isError()" XSLEditorApp.java` returns at least one match (fail-silent guard)
    - The icon wiring block appears BEFORE the line containing `primaryStage.show()` — verify with `grep -n "primaryStage" XSLEditorApp.java` and confirm icons.add line number is less than show() line number
    - `./gradlew build` exits 0 (no compile errors)
  </acceptance_criteria>
  <done>XSLEditorApp.start() loads the icon from classpath, guards with isError(), adds to primaryStage.getIcons(), and logs a warning if the resource is missing — all before primaryStage.show().</done>
</task>

<task type="auto">
  <name>Task 2: Add ImageView icon as first element of VBox in AboutDialog</name>
  <files>src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java</files>
  <read_first>
    - src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java — read in full to see current imports and the exact VBox assembly block (lines 114-117) before editing
  </read_first>
  <action>
    Two changes to AboutDialog.java:

    CHANGE 1 — Add two imports after the existing import block.
    Check current imports before adding — `javafx.geometry.Pos` and `java.io.InputStream` are
    ALREADY imported (lines 5, 21). Add only what is missing:

      import javafx.scene.image.Image;
      import javafx.scene.image.ImageView;

    Do not add `java.io.InputStream` (already present). Do not add `javafx.geometry.Pos` (already present).

    CHANGE 2 — Replace the entire "10. Assemble root VBox" block (lines 114-117) with the
    following (icon load + conditional VBox assembly). Keep comment style consistent with
    existing numbered comments in the constructor:

      // 10a. Optional app icon — fail-silent if resource missing (D-04)
      ImageView iconView = null;
      try (InputStream iconStream =
              getClass().getResourceAsStream("/ch/ti/gagi/xsleditor/icon.png")) {
          if (iconStream != null) {
              Image img = new Image(iconStream);
              if (!img.isError()) {
                  iconView = new ImageView(img);
                  iconView.setFitWidth(64);
                  iconView.setFitHeight(64);
                  iconView.setPreserveRatio(true);
              }
          }
      } catch (Exception ignored) {
          // Dialog must not crash if icon is absent
      }

      // 10b. Assemble root VBox — prepend icon when available
      VBox content;
      if (iconView != null) {
          content = new VBox(8,
              iconView, titleLabel, sep1, stackHeader, grid,
              sep2, authorLabel, licenseBox);
          content.setAlignment(Pos.CENTER);
      } else {
          content = new VBox(8,
              titleLabel, sep1, stackHeader, grid,
              sep2, authorLabel, licenseBox);
      }

    The lines after this block (// 11. Wire DialogPane, getDialogPane().setContent(content),
    getDialogPane().getButtonTypes().add(ButtonType.CLOSE)) remain unchanged.

    Spacing token: VBox gap stays at 8 (matches existing `VBox(8, ...)` — per UI-SPEC.md sm=8px).
    ImageView render size: fitWidth=64, fitHeight=64 (per D-04; matches UI-SPEC.md 3xl=64px render).
  </action>
  <verify>
    <automated>grep -n "ImageView\|iconView\|setFitWidth\|setFitHeight\|setPreserveRatio" ~/Developer/XSLEditor/src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import javafx.scene.image.Image;" AboutDialog.java` returns one match
    - `grep "import javafx.scene.image.ImageView;" AboutDialog.java` returns one match
    - `grep "ImageView iconView" AboutDialog.java` returns one match
    - `grep "setFitWidth(64)" AboutDialog.java` returns one match
    - `grep "setFitHeight(64)" AboutDialog.java` returns one match
    - `grep "setPreserveRatio(true)" AboutDialog.java` returns one match
    - `grep "Pos.CENTER" AboutDialog.java` returns one match (VBox centering when icon present)
    - `grep "isError()" AboutDialog.java` returns one match (fail-silent guard)
    - `grep "iconView, titleLabel" AboutDialog.java` returns one match (icon is first VBox element)
    - `./gradlew build` exits 0 (no compile errors)
  </acceptance_criteria>
  <done>AboutDialog constructor loads the icon fail-silently; if valid, an ImageView (64x64, preserveRatio) is prepended as the first element in the VBox with CENTER alignment. If icon load fails, VBox is assembled without the ImageView and the dialog opens normally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Classpath → JavaFX Image | icon.png bytes read from JAR/resources at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Denial of Service | XSLEditorApp icon load | mitigate | `if (iconStream != null)` + `!icon.isError()` + catch(Exception) — app always launches even if resource is corrupt or missing |
| T-14-05 | Denial of Service | AboutDialog icon load | mitigate | `if (iconStream != null)` + `!img.isError()` + `catch (Exception ignored)` — dialog always opens even without icon |
| T-14-06 | Tampering | icon.png in classpath | accept | Local developer tool; icon is branding-only; no executable content in PNG |
</threat_model>

<verification>
After both tasks complete:
1. `./gradlew build` — exits 0, no compile errors
2. `grep "primaryStage.getIcons().add" src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java` — one match
3. `grep "ImageView iconView" src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java` — one match
4. `grep "setFitWidth(64)" src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java` — one match
5. Line-order check in XSLEditorApp.java: `grep -n "getIcons\|show()" src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java` — getIcons line number must be LESS than show() line number
6. Manual: Launch app — icon visible in macOS window title bar / Dock
7. Manual: Open Help > About — icon visible at top of dialog above the "XSLEditor  v0.3.0" label
</verification>

<success_criteria>
- Application compiles with `./gradlew build` (no errors)
- XSLEditorApp.start() wires icon before primaryStage.show(), with isError() guard and warning log on failure
- About dialog shows icon ImageView (64x64) as the first VBox element, centered, when icon resource is present
- Both icon loads are fail-silent: missing or corrupt icon.png does not crash the app or dialog
- `grep` checks in acceptance_criteria for both tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-version-icon-housekeeping/14-02-SUMMARY.md`
</output>
