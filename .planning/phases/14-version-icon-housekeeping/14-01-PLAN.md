---
phase: 14-version-icon-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - src/main/resources/ch/ti/gagi/xsleditor/icon.png
autonomous: true
requirements:
  - VER-01
  - ICON-02

must_haves:
  truths:
    - "build.gradle declares version = '0.3.0'"
    - "icon.png exists at src/main/resources/ch/ti/gagi/xsleditor/icon.png"
    - "icon.png no longer exists at the project root"
  artifacts:
    - path: "build.gradle"
      provides: "Project version declaration"
      contains: "version = '0.3.0'"
    - path: "src/main/resources/ch/ti/gagi/xsleditor/icon.png"
      provides: "App icon at classpath path /ch/ti/gagi/xsleditor/icon.png"
  key_links:
    - from: "build.gradle version field"
      to: "src/main/resources/version.properties"
      via: "processResources expand(version: project.version)"
      pattern: "version = '0.3.0'"
    - from: "src/main/resources/ch/ti/gagi/xsleditor/icon.png"
      to: "XSLEditorApp.java + AboutDialog.java"
      via: "getClass().getResourceAsStream(\"/ch/ti/gagi/xsleditor/icon.png\")"
      pattern: "icon.png"
---

<objective>
Bump the project version to 0.3.0 and relocate the app icon into the Gradle resources tree.

Purpose: These are prerequisite changes for all downstream work in Phase 14. The version bump causes `processResources` to write "0.3.0" into `version.properties` at build time, which `AboutDialog.loadVersion()` will read at runtime (VER-01). Moving the icon to the classpath resources tree makes it accessible via `getClass().getResourceAsStream()` from both `XSLEditorApp` and `AboutDialog` (ICON-02).

Output: Updated `build.gradle` with `version = '0.3.0'`; `icon.png` moved to `src/main/resources/ch/ti/gagi/xsleditor/icon.png` under git history.
</objective>

<execution_context>
@~/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@~/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@~/Developer/XSLEditor/.planning/ROADMAP.md
@~/Developer/XSLEditor/.planning/phases/14-version-icon-housekeeping/14-CONTEXT.md
@~/Developer/XSLEditor/.planning/phases/14-version-icon-housekeeping/14-PATTERNS.md

<interfaces>
<!-- Key facts extracted from codebase for the executor. No exploration needed. -->

From build.gradle line 13:
  version = '0.1.0'
  → change to: version = '0.3.0'

From build.gradle lines 66-70 (no change needed):
  processResources {
      filesMatching('version.properties') {
          expand(version: project.version)
      }
  }

From src/main/resources/version.properties (no manual edit needed):
  version=${version}
  (expanded to '0.3.0' by processResources at build time)

Icon source: icon.png at project root (~/Developer/XSLEditor/icon.png)
Icon destination: src/main/resources/ch/ti/gagi/xsleditor/icon.png
Classpath path after move: /ch/ti/gagi/xsleditor/icon.png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.3.0 in build.gradle</name>
  <files>build.gradle</files>
  <read_first>
    - build.gradle — read line 13 to confirm current value is `version = '0.1.0'` before editing
  </read_first>
  <action>
    Edit build.gradle line 13. Change:
      version = '0.1.0'
    to:
      version = '0.3.0'

    No other lines in build.gradle change. The `processResources` block (lines 66-70) already
    injects `${version}` into `version.properties` — this single field edit is sufficient
    for `AboutDialog.loadVersion()` to return "0.3.0" at runtime (per D-01).
  </action>
  <verify>
    <automated>grep -n "version = '0.3.0'" ~/Developer/XSLEditor/build.gradle</automated>
  </verify>
  <acceptance_criteria>
    - `grep "version = '0.3.0'" build.gradle` returns exactly one match on line 13
    - `grep "version = '0.1.0'" build.gradle` returns no matches
    - No other lines in build.gradle were modified (verify with `git diff build.gradle` — only line 13 changes)
  </acceptance_criteria>
  <done>build.gradle declares version = '0.3.0' on line 13; all other lines unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Move icon.png into Gradle resources tree</name>
  <files>src/main/resources/ch/ti/gagi/xsleditor/icon.png</files>
  <read_first>
    - Verify icon.png exists at project root before moving: `ls ~/Developer/XSLEditor/icon.png`
    - Verify destination directory exists: `ls ~/Developer/XSLEditor/src/main/resources/ch/ti/gagi/xsleditor/`
  </read_first>
  <action>
    Run git mv to preserve history:

      git mv icon.png src/main/resources/ch/ti/gagi/xsleditor/icon.png

    Working directory: ~/Developer/XSLEditor

    This is a file move only — no code changes in this task. The destination directory
    already exists (contains main.css and main.fxml). After the move, the icon is
    accessible via classpath path `/ch/ti/gagi/xsleditor/icon.png` as required by D-02.
  </action>
  <verify>
    <automated>ls ~/Developer/XSLEditor/src/main/resources/ch/ti/gagi/xsleditor/icon.png &amp;&amp; ! test -f ~/Developer/XSLEditor/icon.png &amp;&amp; echo "PASS: icon moved"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/main/resources/ch/ti/gagi/xsleditor/icon.png` exits 0 (file exists at destination)
    - `test -f icon.png` exits 1 (file no longer at project root)
    - `git status` shows `renamed: icon.png -> src/main/resources/ch/ti/gagi/xsleditor/icon.png` (git mv preserves rename history)
    - No other files added or deleted
  </acceptance_criteria>
  <done>icon.png lives at src/main/resources/ch/ti/gagi/xsleditor/icon.png; project root no longer contains icon.png; git rename recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → Gradle build | version string written into version.properties at build time |
| Classpath → JavaFX runtime | icon.png read from classpath by getClass().getResourceAsStream() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | build.gradle version field | accept | Local desktop tool; no supply-chain verification required; developer owns the build machine |
| T-14-02 | Information Disclosure | icon.png in resources jar | accept | Icon is intentionally public branding asset; no sensitive data |
| T-14-03 | Denial of Service | Missing icon.png at classpath | mitigate | Plan 02 task wires `isError()` check and fail-silent fallback — app must not crash if resource absent |
</threat_model>

<verification>
After both tasks complete:
1. `grep "version = '0.3.0'" build.gradle` — returns one match
2. `ls src/main/resources/ch/ti/gagi/xsleditor/icon.png` — exits 0
3. `test -f icon.png && echo EXISTS || echo GONE` — outputs GONE
4. `git status` — shows only: renamed icon.png and modified build.gradle
5. `./gradlew processResources` followed by `grep version build/resources/main/version.properties` — outputs `version=0.3.0`
</verification>

<success_criteria>
- build.gradle has `version = '0.3.0'` (one occurrence, line 13)
- icon.png is at src/main/resources/ch/ti/gagi/xsleditor/icon.png
- icon.png is absent from the project root
- Running `./gradlew processResources` produces build/resources/main/version.properties containing `version=0.3.0`
</success_criteria>

<output>
After completion, create `.planning/phases/14-version-icon-housekeeping/14-01-SUMMARY.md`
</output>
