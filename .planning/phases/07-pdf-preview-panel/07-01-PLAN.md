---
phase: 07-pdf-preview-panel
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java
autonomous: true
requirements:
  - PREV-03
  - PREV-04

must_haves:
  truths:
    - "Test stubs for PreviewController compile and pass (all @Disabled)"
    - "Test class covers the four state-transition behaviors: D-13 no-banner guard, banner show, banner hide, and first-render placeholder hide"
  artifacts:
    - path: "src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java"
      provides: "Wave 0 test stubs — PreviewController state logic"
      exports: ["PreviewControllerTest"]
  key_links:
    - from: "PreviewControllerTest"
      to: "PreviewController (Wave 1)"
      via: "@Disabled stubs with TODO Wave 1 comments"
      pattern: "@Disabled.*Wave 0"

threat_model:
  trust_boundaries:
    - boundary: "byte[] from render pipeline → WebView temp file"
      description: "Internal pipeline output only; no user-supplied paths or content in this phase"
  stride_register:
    - id: "T-07-01"
      category: "Information Disclosure"
      component: "temp file (xsleditor-preview*.pdf)"
      disposition: "accept"
      rationale: "Local developer tool; temp file contains only the last rendered PDF; OS cleans on reboot"
    - id: "T-07-02"
      category: "Tampering"
      component: "WebView PDF rendering (malformed byte[])"
      disposition: "accept"
      rationale: "bytes come from internal Saxon/FOP pipeline, not user input; malformed PDF is rejected by platform PDF plugin, not a security risk"
---

<objective>
Create Wave 0 test stubs for `PreviewController` covering the two automatable requirements (PREV-03 outdated indicator, PREV-04 no PDF before first render).

Purpose: Establish the RED phase so Wave 1 implementation can target specific test contracts without ambiguity. Tests compile and pass immediately (all @Disabled); Wave 1 enables and fills them.

Output: `PreviewControllerTest.java` with four @Disabled test stubs.
</objective>

<execution_context>
@/Users/kraehend/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraehend/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kraehend/Developer/XSLEditor/.planning/PROJECT.md
@/Users/kraehend/Developer/XSLEditor/.planning/ROADMAP.md
@/Users/kraehend/Developer/XSLEditor/.planning/phases/07-pdf-preview-panel/07-CONTEXT.md
@/Users/kraehend/Developer/XSLEditor/.planning/phases/07-pdf-preview-panel/07-PATTERNS.md
@/Users/kraehend/Developer/XSLEditor/.planning/phases/07-pdf-preview-panel/07-VALIDATION.md

<interfaces>
<!-- Analog test pattern — copy @BeforeAll block exactly. Source: RenderControllerTest.java lines 1–27 -->

```java
// package / imports skeleton (from RenderControllerTest):
package ch.ti.gagi.xsleditor.ui;

import javafx.application.Platform;
import javafx.scene.control.Label;
import javafx.scene.layout.StackPane;
import javafx.scene.web.WebView;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

// @BeforeAll guard (established pattern — EditorTabTest + RenderControllerTest):
@BeforeAll
static void initJavaFxToolkit() {
    try {
        Platform.startup(() -> { });
    } catch (IllegalStateException alreadyStarted) {
        // Toolkit was already initialised by a previous test class — OK.
    }
}
```

<!-- PreviewController public surface (Wave 1 will implement these) -->
```java
// src/main/java/ch/ti/gagi/xsleditor/ui/PreviewController.java
public final class PreviewController {
    public void initialize(StackPane previewPane, WebView previewWebView,
                           Label previewPlaceholderLabel, Label outdatedBannerLabel);
    public void displayPdf(byte[] bytes);         // PREV-04: first call hides placeholder, shows WebView
    public void setOutdated(boolean outdated);    // PREV-03: shows/hides outdatedBannerLabel
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create PreviewControllerTest with four @Disabled stubs</name>
  <files>src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java</files>
  <read_first>
    - src/test/java/ch/ti/gagi/xsleditor/ui/RenderControllerTest.java (copy Platform.startup() guard and @Disabled/@Test structure exactly)
    - .planning/phases/07-pdf-preview-panel/07-PATTERNS.md (test method patterns and names)
    - .planning/phases/07-pdf-preview-panel/07-VALIDATION.md (wave 0 requirements)
  </read_first>
  <behavior>
    Four test stubs, each @Disabled with "Wave 0 stub — implement in Wave 1 (07-02-PLAN.md)":

    1. setOutdated_doesNothing_whenHasPdfIsFalse
       - Covers D-13: setOutdated(true) before any displayPdf() call must NOT make the banner visible
       - TODO comment: create PreviewController, call initialize() with Label/StackPane/WebView, call
         setOutdated(true), assert outdatedBannerLabel.isVisible() == false

    2. setOutdated_showsBanner_whenHasPdfIsTrue
       - Covers PREV-03 / D-04: after a successful render (hasPdf=true), setOutdated(true) must show banner
       - TODO comment: create PreviewController, call initialize(), call displayPdf(new byte[1]) to set
         hasPdf=true, call setOutdated(true), assert outdatedBannerLabel.isVisible() == true

    3. setOutdated_false_hidesBanner
       - Covers D-06: setOutdated(false) must hide banner regardless of prior state
       - TODO comment: create PreviewController, put it in shown-banner state, call setOutdated(false),
         assert outdatedBannerLabel.isVisible() == false

    4. displayPdf_hidesPlaceholderAndShowsWebView
       - Covers PREV-04 / D-11: displayPdf(byte[]) must hide previewPlaceholderLabel and show previewWebView
       - TODO comment: create PreviewController in initial state (WebView managed=false/visible=false,
         placeholder managed=true/visible=true), call displayPdf(new byte[1]), assert
         previewWebView.isVisible() == true AND previewPlaceholderLabel.isVisible() == false
       - Note: WebView instantiation requires FX thread — wrap in Platform.runLater() + CountDownLatch
         (same pattern as EditorTabTest)
  </behavior>
  <action>
    Create the file at the exact path. Use the package declaration `package ch.ti.gagi.xsleditor.ui;`.

    Imports required (per PATTERNS.md):
    - javafx.application.Platform
    - javafx.scene.control.Label
    - javafx.scene.layout.StackPane
    - javafx.scene.web.WebView
    - org.junit.jupiter.api.BeforeAll
    - org.junit.jupiter.api.Disabled
    - org.junit.jupiter.api.Test
    - static org.junit.jupiter.api.Assertions.*

    Class Javadoc: "Unit tests for PreviewController state-transition logic. Wave 0 stubs — Wave 1 enables and implements. PREV-03, PREV-04."

    @BeforeAll: copy Platform.startup() guard exactly from RenderControllerTest (lines 20–26).

    Each @Test method body: only `fail("Not yet implemented");` (the TODO Wave 1 comment goes in the
    method-level Javadoc or as a // TODO comment before the fail() call, not as the only content).

    @Disabled value for all four stubs: `"Wave 0 stub — implement in Wave 1 (07-02-PLAN.md)"`
  </action>
  <verify>
    <automated>./gradlew test --tests "ch.ti.gagi.xsleditor.ui.PreviewControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java
    - `grep -c "@Disabled" src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java` returns 4
    - `grep "setOutdated_doesNothing_whenHasPdfIsFalse" src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java` finds match
    - `grep "setOutdated_showsBanner_whenHasPdfIsTrue" src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java` finds match
    - `grep "setOutdated_false_hidesBanner" src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java` finds match
    - `grep "displayPdf_hidesPlaceholderAndShowsWebView" src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java` finds match
    - `grep "Platform.startup" src/test/java/ch/ti/gagi/xsleditor/ui/PreviewControllerTest.java` finds match
    - `./gradlew test --tests "ch.ti.gagi.xsleditor.ui.PreviewControllerTest"` exits 0 (all stubs skipped, none fail)
  </acceptance_criteria>
  <done>PreviewControllerTest.java exists with four @Disabled stubs; full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| byte[] pipeline output → temp file | Internal FOP render output written to OS temp dir; no user-supplied path component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | temp file (xsleditor-preview*.pdf) | accept | Local developer tool; last rendered PDF in /tmp; OS cleans on reboot; no PII risk |
| T-07-02 | Tampering | WebView PDF rendering (malformed bytes) | accept | bytes from internal Saxon/FOP pipeline only; malformed PDF rejected by platform PDF plugin; no user-controlled input path |
</threat_model>

<verification>
Run: `./gradlew test --tests "ch.ti.gagi.xsleditor.ui.PreviewControllerTest"`

Expected: BUILD SUCCESS, 4 tests skipped (all @Disabled), 0 failures.
</verification>

<success_criteria>
- PreviewControllerTest.java created with correct package and four @Disabled stubs
- `./gradlew test` exits 0 (no new failures introduced)
- Test names match VALIDATION.md task map (7-01-01 covered)
</success_criteria>

<output>
After completion, create `.planning/phases/07-pdf-preview-panel/07-01-SUMMARY.md`
</output>
