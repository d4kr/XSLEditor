---
phase: 18
plan: "18-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/screenshot.png
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "README.md contains a project overview explaining the XML → XSLT → XSL-FO → PDF pipeline"
    - "README.md contains a Prerequisites section listing Java 21 as a requirement"
    - "README.md contains the exact build command: ./gradlew shadowJar"
    - "README.md contains the exact run command: java -jar build/libs/XSLEditor-0.3.0.jar"
    - "README.md states version 0.3.0 in at least one visible location"
    - "README.md lists all four core stack items: Java 21, Saxon-HE 12.4, Apache FOP 2.9, JavaFX 21"
    - "README.md includes a reference to the app icon image (icon.png)"
    - "README.md includes a screenshot reference pointing to docs/screenshot.png"
    - "docs/screenshot.png exists and is a valid PNG file captured from the running app"
  artifacts:
    - path: "README.md"
      provides: "Full project documentation for a developer arriving fresh"
      contains: "Prerequisites, build command, run command, tech stack, screenshot"
    - path: "docs/screenshot.png"
      provides: "Visual proof of the three-panel UI (file tree, editor, PDF preview)"
  key_links:
    - from: "README.md"
      to: "src/main/resources/ch/ti/gagi/xsleditor/icon.png"
      via: "img tag or markdown image link"
      pattern: "icon\\.png"
    - from: "README.md"
      to: "docs/screenshot.png"
      via: "markdown image link"
      pattern: "docs/screenshot\\.png"
---

<objective>
Rewrite README.md from its current 4-line stub into a complete developer-facing document that covers project overview, prerequisites, build/run instructions, tech stack, and visual assets (icon + screenshot). Also capture the screenshot of the running application.

Purpose: DOC-01 (build/run completeness), DOC-02 (icon + screenshot), DOC-03 (version + stack accuracy). A developer with Java 21 installed must be able to build and run XSLEditor from the README alone.

Output:
- README.md — full rewrite
- docs/screenshot.png — captured from the running app showing the three-panel layout
</objective>

<execution_context>
@/Users/kraehend/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraehend/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kraehend/Developer/XSLEditor/.planning/PROJECT.md
@/Users/kraehend/Developer/XSLEditor/.planning/ROADMAP.md
@/Users/kraehend/Developer/XSLEditor/CLAUDE.md
@/Users/kraehend/Developer/XSLEditor/build.gradle
@/Users/kraehend/Developer/XSLEditor/README.md

<interfaces>
<!-- Key facts from build.gradle and existing codebase — executor must use these exact values. -->

From build.gradle:
  version = '0.3.0'
  mainClass = 'ch.ti.gagi.xsleditor.XSLEditorApp'
  java toolchain: languageVersion = JavaLanguageVersion.of(21)
  javafx version = '21', modules = ['javafx.controls', 'javafx.fxml', 'javafx.web']
  Saxon-HE: 'net.sf.saxon:Saxon-HE:12.4'
  Apache FOP: 'org.apache.xmlgraphics:fop:2.9'
  RichTextFX: 'org.fxmisc.richtext:richtextfx:0.11.5'
  PDFBox: 'org.apache.pdfbox:pdfbox:2.0.31'
  Jackson: 'com.fasterxml.jackson.core:jackson-databind:2.17.2'
  Shadow JAR output: build/libs/XSLEditor-0.3.0.jar  (archiveClassifier = '' so no suffix)

Icon file: src/main/resources/ch/ti/gagi/xsleditor/icon.png
Docs dir:  docs/  (already exists, contains PRD.md and SPEC.md)

Pipeline: XML → XSLT → XSL-FO → PDF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — 4 lines, to be fully replaced)
    - build.gradle (exact versions: 0.3.0, Saxon-HE 12.4, FOP 2.9, JavaFX 21, RichTextFX 0.11.5)
    - CLAUDE.md (project description for the overview section)
  </read_first>
  <action>
Overwrite README.md with the full content below. Use this exact structure and concrete values — do not paraphrase or omit sections.

The README must contain all of the following sections in this order:

---

# XSLEditor

> Local desktop tool for developers to edit multi-file XSLT/XSL-FO templates, generate PDFs on demand, and debug the full pipeline — all in one window.

![App icon](src/main/resources/ch/ti/gagi/xsleditor/icon.png)

**Version:** 0.3.0

## Overview

XSLEditor implements the complete XML → XSLT → XSL-FO → PDF pipeline in a single desktop application. There is no external backend, no network dependency, and no authentication — everything runs locally on the developer's machine.

Core goals:
- Edit-to-preview cycle under 5 seconds
- No context switching: file tree, code editor, and PDF preview in one window
- Real-time XML validation with error navigation (click error → jump to source line)
- Multi-file project support with dependency tracking
- Full pipeline error reporting: XML syntax, XSLT compile, XSLT runtime, XSL-FO rendering

## Screenshot

![XSLEditor main window](docs/screenshot.png)

*Three-panel layout: file tree (left), code editor (center), PDF preview (right)*

## Prerequisites

- **Java 21** or later — [Download from Adoptium](https://adoptium.net/)
- No other runtime dependencies: Saxon, FOP, JavaFX, and all libraries are bundled in the shadow JAR.

## Build

```bash
./gradlew shadowJar
```

Output: `build/libs/XSLEditor-0.3.0.jar`

## Run

```bash
java -jar build/libs/XSLEditor-0.3.0.jar
```

macOS note: if you see a Gatekeeper warning, right-click the JAR and choose Open, or run:

```bash
xattr -d com.apple.quarantine build/libs/XSLEditor-0.3.0.jar
```

## Tech Stack

| Component | Library | Version |
|-----------|---------|---------|
| Language | Java | 21 |
| UI framework | JavaFX (controls, fxml, web) | 21 |
| Code editor widget | RichTextFX | 0.11.5 |
| XSLT processor | Saxon-HE | 12.4 |
| XSL-FO → PDF renderer | Apache FOP | 2.9 |
| PDF page rendering | Apache PDFBox | 2.0.31 |
| Project config (JSON) | Jackson Databind | 2.17.2 |
| Build & packaging | Gradle 9 + Shadow JAR | — |

## Project Structure

```
src/main/java/ch/ti/gagi/xsleditor/
  XSLEditorApp.java          — JavaFX Application entry point
  controller/                — FXML controllers (editor, preview, log panel)
  model/                     — Project model, file management
  render/                    — XSLT + FOP pipeline orchestration
  util/                      — Encoding detection, preprocessing directives
src/main/resources/
  ch/ti/gagi/xsleditor/
    *.fxml                   — Layout files
    dark-theme.css           — Application stylesheet
    icon.png                 — Application icon
    version.properties       — Version string injected at build time
```

## Development Notes

- Build system: Gradle 9 with the `com.gradleup.shadow` plugin (gradleup fork — required for Gradle 9 compatibility)
- All pipeline stages run in-process; no subprocess spawning
- Project files are stored as `.xsle-tool.json` alongside the project's XSLT files
- Custom preprocessing directives (`<?LIBRARY ...?>`) are resolved before the XSLT pipeline runs

## License

Internal developer tool — not distributed publicly.

---

END OF README CONTENT.

Write the above content verbatim (substituting the exact versions from build.gradle). The screenshot image `docs/screenshot.png` is added in Task 2 (human step), so the image link in the README will be a broken reference until Task 2 completes — that is expected.
  </action>
  <verify>
    <automated>grep -n "0\.3\.0" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "Saxon-HE 12\.4" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "Apache FOP 2\.9" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "JavaFX" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "\./gradlew shadowJar" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "Prerequisites" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "docs/screenshot\.png" /Users/kraehend/Developer/XSLEditor/README.md &amp;&amp; grep -n "icon\.png" /Users/kraehend/Developer/XSLEditor/README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "0\.3\.0" README.md returns at least one match (version stated explicitly)
    - grep "Saxon-HE 12\.4" README.md returns a match in the tech stack table
    - grep "Apache FOP 2\.9" README.md returns a match
    - grep "JavaFX" README.md returns a match (with version 21)
    - grep "\./gradlew shadowJar" README.md returns a match
    - grep "java -jar build/libs/XSLEditor-0\.3\.0\.jar" README.md returns a match
    - grep "Prerequisites" README.md returns a match (section header present)
    - grep "docs/screenshot\.png" README.md returns a match (screenshot img reference present)
    - grep "icon\.png" README.md returns a match (icon img reference present)
    - grep "Java 21" README.md returns a match (prerequisite stated)
    - wc -l README.md returns at least 80 (document is substantive, not a stub)
  </acceptance_criteria>
  <done>README.md is a complete developer document covering overview, prerequisites (Java 21), build command (./gradlew shadowJar), run command (java -jar build/libs/XSLEditor-0.3.0.jar), full tech stack table with exact versions, and references to both icon.png and docs/screenshot.png.</done>
</task>

<task type="auto">
  <name>Task 2: Build the app so it is ready to screenshot</name>
  <files>build/libs/XSLEditor-0.3.0.jar</files>
  <read_first>
    - build.gradle (confirm shadowJar task name and output artifact path)
  </read_first>
  <action>
Run the Gradle shadow JAR build so the executable JAR is ready for the screenshot step.

Command:
  cd /Users/kraehend/Developer/XSLEditor && ./gradlew shadowJar

Expected output artifact: build/libs/XSLEditor-0.3.0.jar

If the build fails, read the error output and fix the issue before proceeding. Do not continue to the screenshot checkpoint if the JAR does not exist.
  </action>
  <verify>
    <automated>ls -lh /Users/kraehend/Developer/XSLEditor/build/libs/XSLEditor-0.3.0.jar</automated>
  </verify>
  <acceptance_criteria>
    - ls build/libs/XSLEditor-0.3.0.jar exits with code 0 (file exists)
    - File size is greater than 1 MB (shadow JAR with bundled dependencies is always large)
  </acceptance_criteria>
  <done>build/libs/XSLEditor-0.3.0.jar exists and is larger than 1 MB, confirming the shadow JAR built successfully with all dependencies bundled.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Capture screenshot of the running app</name>
  <what-built>Task 2 produced build/libs/XSLEditor-0.3.0.jar — the app is ready to run.</what-built>
  <how-to-verify>
1. Launch the app:
   ```
   java -jar /Users/kraehend/Developer/XSLEditor/build/libs/XSLEditor-0.3.0.jar
   ```

2. Open a project (File → Open Project, or use an existing project you have on disk).
   The goal is to show all three panels populated:
   - Left panel: file tree with at least one file visible
   - Center panel: code editor with an open XSLT/XML file and syntax highlighting
   - Right panel: PDF preview with a rendered page (trigger a render if needed)

3. Resize the window so all three panels are clearly visible without overlap.

4. Take a screenshot using macOS:
   - Press Cmd+Shift+4 and drag to capture the app window, OR
   - Press Cmd+Shift+4 then Space then click the window for a clean window capture

5. Move or copy the captured screenshot to:
   `/Users/kraehend/Developer/XSLEditor/docs/screenshot.png`

   (macOS saves screenshots to Desktop by default as `Screenshot YYYY-MM-DD at HH.MM.SS.png`)

   Example move command:
   ```bash
   mv ~/Desktop/Screenshot\ *.png /Users/kraehend/Developer/XSLEditor/docs/screenshot.png
   ```
   Rename/copy it explicitly to `screenshot.png` — the README references this exact path.

6. Verify the file exists:
   ```bash
   ls -lh /Users/kraehend/Developer/XSLEditor/docs/screenshot.png
   ```

7. Quit the app.
  </how-to-verify>
  <resume-signal>Type "screenshot done" once docs/screenshot.png is in place, or describe any issues encountered (app did not launch, no project to open, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → local filesystem | Icon and screenshot are referenced via relative paths; if paths drift the images break silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | README.md | accept | README is documentation for an internal tool; no credentials, secrets, or PII are included |
| T-18-02 | Tampering | docs/screenshot.png | accept | Static image asset, no executable code; no integrity check needed for documentation |
</threat_model>

<verification>
After both tasks complete and the human checkpoint is cleared:

```bash
# 1. README has all required content
grep "0\.3\.0" /Users/kraehend/Developer/XSLEditor/README.md
grep "Saxon-HE 12\.4" /Users/kraehend/Developer/XSLEditor/README.md
grep "Apache FOP 2\.9" /Users/kraehend/Developer/XSLEditor/README.md
grep "Java 21" /Users/kraehend/Developer/XSLEditor/README.md
grep "\./gradlew shadowJar" /Users/kraehend/Developer/XSLEditor/README.md
grep "java -jar build/libs/XSLEditor-0\.3\.0\.jar" /Users/kraehend/Developer/XSLEditor/README.md
grep "Prerequisites" /Users/kraehend/Developer/XSLEditor/README.md
grep "docs/screenshot\.png" /Users/kraehend/Developer/XSLEditor/README.md
grep "icon\.png" /Users/kraehend/Developer/XSLEditor/README.md

# 2. Screenshot exists
ls -lh /Users/kraehend/Developer/XSLEditor/docs/screenshot.png

# 3. README is substantive (not a stub)
wc -l /Users/kraehend/Developer/XSLEditor/README.md
# Expected: 80+ lines
```

All commands must exit 0 and return matching content.
</verification>

<success_criteria>
- DOC-01: README contains project overview, Prerequisites section (Java 21), ./gradlew shadowJar build command, java -jar run command — a developer with Java 21 can build and run from the README alone
- DOC-02: README img references point to icon.png (src/main/resources/ch/ti/gagi/xsleditor/icon.png) and docs/screenshot.png; docs/screenshot.png exists as a real PNG captured from the running app showing file tree, editor, and PDF preview
- DOC-03: README states version 0.3.0 and lists Java 21, Saxon-HE 12.4, Apache FOP 2.9, JavaFX 21, RichTextFX 0.11.5 in the tech stack table
</success_criteria>

<output>
After all tasks complete (including human screenshot checkpoint), create:
`.planning/phases/18-readme-rewrite/18-01-SUMMARY.md`

Use the summary template at:
@/Users/kraehend/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</output>
