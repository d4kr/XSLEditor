---
phase: 11-about-dialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - src/main/resources/version.properties
  - src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java
autonomous: true
requirements:
  - ABOUT-02

must_haves:
  truths:
    - "The build produces a version.properties file on the classpath containing the correct version string from build.gradle"
    - "XLSEditorApp exposes a static getHostServices() accessor that returns the JavaFX HostServices instance"
    - "build.gradle version field reads '0.1.0' (not '1.0.0')"
  artifacts:
    - path: "src/main/resources/version.properties"
      provides: "Gradle-expanded version string at classpath root"
      contains: "version=${version}"
    - path: "build.gradle"
      provides: "Corrected version + processResources expansion block"
      contains: "version = '0.1.0'"
    - path: "src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java"
      provides: "Static HostServices accessor"
      exports: ["getHostServices()"]
  key_links:
    - from: "build.gradle processResources block"
      to: "src/main/resources/version.properties"
      via: "Gradle expand() token substitution"
      pattern: "filesMatching.*version\\.properties"
    - from: "src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java start()"
      to: "hostServicesInstance field"
      via: "hostServicesInstance = getHostServices()"
      pattern: "hostServicesInstance\\s*=\\s*getHostServices"
---

<objective>
Lay the infrastructure that AboutDialog depends on: correct the build.gradle version from '1.0.0'
to '0.1.0' (D-02), add a Gradle processResources block that writes the version into a
version.properties file at classpath root (D-01), create that resource file with the Gradle
expand() placeholder, and add a static HostServices accessor to XLSEditorApp so AboutDialog can
call the browser-open API (D-05).

Purpose: These three changes are independent of the dialog UI and can ship in Wave 1. No other
plan can execute until version.properties exists and HostServices is accessible.
Output: build.gradle updated, version.properties created, XLSEditorApp updated.
</objective>

<execution_context>
@/Users/kraehend/Developer/XLSEditor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraehend/Developer/XLSEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kraehend/Developer/XLSEditor/.planning/PROJECT.md
@/Users/kraehend/Developer/XLSEditor/.planning/ROADMAP.md
@/Users/kraehend/Developer/XLSEditor/.planning/phases/11-about-dialog/11-CONTEXT.md

<interfaces>
<!-- Extracted from build.gradle (current state — executor MUST read before editing) -->
<!-- build.gradle line 13 (current — WRONG): version = '1.0.0' -->
<!-- build.gradle line 62-64 (test block, insert processResources AFTER): -->
<!--   test { -->
<!--       useJUnitPlatform() -->
<!--   } -->

<!-- Extracted from XLSEditorApp.java (current state — executor MUST read before editing) -->
<!-- Line 23: public static final String APP_NAME = "XLSEditor"; -->
<!-- Line 26: public void start(Stage primaryStage) throws IOException { -->
<!-- Line 42:     primaryStage.show();   <-- assign hostServicesInstance BEFORE this line -->
<!-- Line 48: public static void main(String[] args) { launch(args); } -->

<!-- After this plan, XLSEditorApp will expose: -->
<!-- private static javafx.application.HostServices hostServicesInstance; -->
<!-- public static javafx.application.HostServices getHostServices() { return hostServicesInstance; } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct version in build.gradle and add processResources block</name>
  <files>build.gradle</files>
  <read_first>
    - build.gradle (full file — executor must see current structure before editing)
  </read_first>
  <action>
    Make two edits to build.gradle:

    1. Change line 13 from:
         version = '1.0.0'
       to:
         version = '0.1.0'
       (per D-02 — 0.x versioning schema; MVP retroactively becomes 0.1.0)

    2. Append a processResources block after the closing brace of the `test { }` block (currently
       ending at line 64). The new block:
         processResources {
             filesMatching('version.properties') {
                 expand(version: project.version)
             }
         }
       This instructs Gradle to substitute `${version}` in any file named version.properties with
       the value of project.version at build time. The expand() call uses Groovy GString syntax —
       the placeholder in the resource file is written as `${version}` (standard Gradle expand).

    Do NOT touch any other field in build.gradle.
  </action>
  <verify>
    <automated>grep "version = '0.1.0'" /Users/kraehend/Developer/XLSEditor/build.gradle && grep "filesMatching" /Users/kraehend/Developer/XLSEditor/build.gradle && grep "expand" /Users/kraehend/Developer/XLSEditor/build.gradle</automated>
  </verify>
  <acceptance_criteria>
    - grep "version = '0.1.0'" build.gradle exits 0
    - grep "filesMatching('version.properties')" build.gradle exits 0
    - grep "expand(version: project.version)" build.gradle exits 0
    - grep "version = '1.0.0'" build.gradle exits non-zero (old value removed)
  </acceptance_criteria>
  <done>build.gradle has version '0.1.0' and processResources block that expands version.properties</done>
</task>

<task type="auto">
  <name>Task 2: Create version.properties and expose HostServices in XLSEditorApp</name>
  <files>
    src/main/resources/version.properties
    src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java
  </files>
  <read_first>
    - src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java (full file — executor must see current state before editing)
  </read_first>
  <action>
    Step A — Create src/main/resources/version.properties (new file, no analog):
      Content is exactly one line:
        version=${version}
      Place at src/main/resources/version.properties (classpath root, NOT under the package
      subdirectory). This path ensures `getClass().getResourceAsStream("/version.properties")`
      resolves from any class in the project. The ${version} token is the Gradle expand()
      placeholder — do not use a literal version string here.

    Step B — Edit XLSEditorApp.java:
      1. Add a private static field immediately after the APP_NAME constant (line 23):
           private static javafx.application.HostServices hostServicesInstance;
         Use the fully-qualified type to avoid adding a new import (Application.getHostServices()
         returns javafx.application.HostServices which is already in scope via javafx.application.Application).

      2. Add a public static accessor after the field declaration:
           public static javafx.application.HostServices getHostServices() {
               return hostServicesInstance;
           }
         Note: this overloads the inherited Application.getHostServices() as a static accessor.
         Name it differently to avoid the overload ambiguity — rename to `hostServices()`:
           public static javafx.application.HostServices hostServices() {
               return hostServicesInstance;
           }
         MainController will call XLSEditorApp.hostServices() (not getHostServices()) to avoid
         shadowing the inherited instance method.

      3. Inside start(), add the assignment BEFORE `primaryStage.show()` (currently line 42):
           hostServicesInstance = getHostServices();
         This calls Application.getHostServices() (inherited from javafx.application.Application)
         and stores it in the static field so it is accessible after the Application instance is
         no longer in scope.

      Do NOT change any other line in XLSEditorApp.java.
  </action>
  <verify>
    <automated>
      test -f /Users/kraehend/Developer/XLSEditor/src/main/resources/version.properties &&
      grep 'version=\${version}' /Users/kraehend/Developer/XLSEditor/src/main/resources/version.properties &&
      grep 'hostServicesInstance' /Users/kraehend/Developer/XLSEditor/src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java &&
      grep 'hostServices()' /Users/kraehend/Developer/XLSEditor/src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java &&
      cd /Users/kraehend/Developer/XLSEditor && ./gradlew compileJava -q 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - src/main/resources/version.properties exists
    - grep 'version=${version}' version.properties exits 0 (template placeholder, not literal version)
    - grep 'hostServicesInstance' XLSEditorApp.java exits 0
    - grep 'public static javafx.application.HostServices hostServices()' XLSEditorApp.java exits 0
    - grep 'hostServicesInstance = getHostServices()' XLSEditorApp.java exits 0 (assignment in start())
    - ./gradlew compileJava succeeds with no errors
  </acceptance_criteria>
  <done>version.properties template created at classpath root; XLSEditorApp exposes static hostServices() accessor assigned in start()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Classpath resource read | version.properties is read from the JAR classpath at runtime — content is fixed at build time, no user input crosses this boundary |
| HostServices static field | Accessed only from the JavaFX Application Thread after start() completes — no cross-thread risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | version.properties at classpath root | accept | File is baked into the JAR at build time by Gradle; runtime modification not possible in a fat-JAR deployment |
| T-11-02 | Information Disclosure | hostServicesInstance static field | accept | Field is package-private by scoping (private static); accessor is public but returns only the JavaFX platform HostServices handle — no credentials or PII involved |
| T-11-03 | Denial of Service | processResources expand() failing | accept | If expand fails at build time the Gradle task fails loudly; no silent corruption; developer sees the error immediately |
</threat_model>

<verification>
1. `grep "version = '0.1.0'" build.gradle` — confirms version corrected (D-02)
2. `grep "filesMatching" build.gradle` — confirms processResources block present (D-01)
3. `test -f src/main/resources/version.properties` — resource file exists
4. `grep 'version=${version}' src/main/resources/version.properties` — placeholder not hardcoded
5. `grep 'hostServices()' src/main/java/ch/ti/gagi/xlseditor/XLSEditorApp.java` — static accessor present
6. `./gradlew compileJava` — compilation succeeds
</verification>

<success_criteria>
- build.gradle version corrected to '0.1.0'
- processResources block expands version.properties at build time
- src/main/resources/version.properties exists with ${version} placeholder
- XLSEditorApp.hostServices() static accessor returns the live HostServices handle
- ./gradlew compileJava green
</success_criteria>

<output>
After completion, create `.planning/phases/11-about-dialog/11-01-SUMMARY.md`
</output>
