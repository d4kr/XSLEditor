---
phase: 23-signing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SIGNING.md
  - README.md
  - .github/workflows/release.yml
autonomous: true
requirements:
  - SIGN-01
tags:
  - documentation
  - signing
  - macos
  - ci

must_haves:
  truths:
    - "A new maintainer can read docs/SIGNING.md and configure all 7 macOS signing secrets without consulting other files"
    - "docs/SIGNING.md names all 7 secrets and explains what each expects"
    - "docs/SIGNING.md states the Apple Developer Program $99/yr requirement prominently"
    - "docs/SIGNING.md notes the Windows MSI is unsigned and explains the SmartScreen warning"
    - "docs/SIGNING.md gives a concrete verify step (push pre-release tag v0.4.0-test1)"
    - "README.md links to docs/SIGNING.md from a Contributing / Release Setup section"
  artifacts:
    - path: "docs/SIGNING.md"
      provides: "Maintainer guide for macOS signing + notarization secret configuration"
      contains_all:
        - "MACOS_CERTIFICATE"
        - "MACOS_CERTIFICATE_PASSWORD"
        - "MACOS_SIGNING_IDENTITY"
        - "MACOS_KEYCHAIN_PASSWORD"
        - "APPLE_ID"
        - "APPLE_TEAM_ID"
        - "APPLE_APP_SPECIFIC_PASSWORD"
        - "Apple Developer Program"
        - "$99"
        - "SmartScreen"
        - "v0.4.0-test1"
        - "base64 -i"
        - "openssl rand -hex 16"
    - path: "README.md"
      provides: "Cross-link to SIGNING.md from a Contributing / Release Setup section"
      contains: "## Contributing / Release Setup"
    - path: ".github/workflows/release.yml"
      provides: "Pointer comment referencing docs/SIGNING.md"
      contains: "docs/SIGNING.md"
  key_links:
    - from: "README.md"
      to: "docs/SIGNING.md"
      via: "relative markdown link"
      pattern: "\\[`docs/SIGNING.md`\\]\\(docs/SIGNING.md\\)"
    - from: ".github/workflows/release.yml"
      to: "docs/SIGNING.md"
      via: "header pointer comment"
      pattern: "# Full setup guide: docs/SIGNING.md"
---

<objective>
Create `docs/SIGNING.md` — a self-contained maintainer guide for configuring all 7 macOS signing + notarization GitHub Actions secrets, then make it discoverable from `README.md` and `.github/workflows/release.yml`.

Purpose: A new repository maintainer must be able to set up the full signing + notarization pipeline by following ONE document, without spelunking through CI workflow comments or commit history. This unblocks any future maintainer from re-creating the signing infrastructure on a fresh fork or after secret rotation.

Output:
- `docs/SIGNING.md` (new) — numbered step-by-step guide with verbatim secrets reference block from `release.yml`
- `README.md` (modified) — new "## Contributing / Release Setup" section linking to the guide
- `.github/workflows/release.yml` (modified) — single header pointer comment to the guide
</objective>

<execution_context>
@~/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@~/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-signing-documentation/23-CONTEXT.md
@.planning/phases/23-signing-documentation/23-PATTERNS.md
@README.md
@.github/workflows/release.yml
@docs/PRD.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/SIGNING.md with full secrets guide</name>
  <files>docs/SIGNING.md</files>
  <read_first>
    - .github/workflows/release.yml (lines 1-50 — copy header secrets comment block verbatim)
    - README.md (lines 1-91 — match heading style, fenced code style, table style, prose-note style)
    - .planning/phases/23-signing-documentation/23-CONTEXT.md (decisions D-01 through D-04, specifics)
    - .planning/phases/23-signing-documentation/23-PATTERNS.md (SIGNING.md content outline)
  </read_first>
  <action>
Create new file `docs/SIGNING.md` with the following exact structure (per D-01, D-02, D-03, D-04 and the content outline in 23-PATTERNS.md):

1. H1 title: `# macOS Signing & Notarization — Maintainer Guide`
2. Blockquote subtitle (one sentence) describing what the guide enables — match README.md line 3 style.
3. Prerequisite callout (bold-prefixed prose note, NOT GitHub admonition syntax) placed immediately after the subtitle, stating verbatim: "**Prerequisite:** An active Apple Developer Program membership ($99/yr) is required for code signing and notarization. Sign up at https://developer.apple.com/programs/."
4. `## Overview` section — 2-3 sentences explaining: this guide configures the 7 GitHub Actions secrets the release workflow needs to sign and notarize macOS DMGs.
5. `## Setup Steps` section with numbered steps. Each step is a `### Step N: Title` H3 with prose + a fenced `bash` code block where applicable. Steps:
   - Step 1: Export the Developer ID Application certificate as `.p12` from Keychain Access. Prose: "Open Keychain Access → expand the `login` keychain → find the entry named `Developer ID Application: <Your Name> (<TEAM_ID>)` → right-click → Export → save as `cert.p12` with a strong password (you will need this password for `MACOS_CERTIFICATE_PASSWORD`)."
   - Step 2: Base64-encode the `.p12`. Fenced bash block:
     ```bash
     base64 -i /path/to/cert.p12 | pbcopy
     ```
     Prose: "The encoded value is now on your clipboard — paste it as the value of the `MACOS_CERTIFICATE` secret."
   - Step 3: Generate a random keychain password. Fenced bash block:
     ```bash
     openssl rand -hex 16
     ```
     Prose: "Use the output as the value of `MACOS_KEYCHAIN_PASSWORD`. This password protects an ephemeral keychain that is created and destroyed on every CI run."
   - Step 4: Obtain your Apple Team ID. Prose: "Visit https://developer.apple.com/account → the 10-character ID under your name (e.g. `ABC123XYZ7`) is the value for `APPLE_TEAM_ID`."
   - Step 5: Generate an App-Specific Password. Prose: "Visit https://appleid.apple.com → Sign-In and Security → App-Specific Passwords → Generate. Use the resulting password for `APPLE_APP_SPECIFIC_PASSWORD`. Use your Apple ID email for `APPLE_ID`."
   - Step 6: Determine `MACOS_SIGNING_IDENTITY`. Bold callout immediately under this heading: "**Gotcha:** `MACOS_SIGNING_IDENTITY` is the NAME PORTION ONLY of the certificate subject — NOT the full string." Then show:
     ```text
     Correct:   Acme Corp
     Incorrect: Developer ID Application: Acme Corp (ABC123XYZ7)
     ```
     Prose: "jpackage uses `--mac-signing-key-user-name` which expects only the name portion."
   - Step 7: Add all 7 secrets in GitHub. Prose: "Navigate to: GitHub → Settings → Secrets and variables → Actions → New repository secret. Add each of the 7 secrets listed below."
6. `## Secrets Reference` section. Single sentence introducing it: "The release workflow's authoritative secret specifications (reproduced from `.github/workflows/release.yml` per the single-source-of-truth principle):". Then a fenced ```yaml block containing the EXACT verbatim copy of `.github/workflows/release.yml` lines 1–41 (the entire `# =====` banner-bracketed `# Required GitHub Actions Secrets` block including all 7 secret descriptions). Do NOT paraphrase — copy the comment block character-for-character.
7. `## Verify Your Setup` section (per D-03). Prose: "Push a pre-release tag to trigger the release workflow without producing a public release:"
   ```bash
   git tag v0.4.0-test1
   git push origin v0.4.0-test1
   ```
   Then: "The `-` suffix in `v0.4.0-test1` causes the release to be auto-marked as pre-release. Watch the workflow at GitHub → Actions → Release. The `package-macos-arm` and `package-macos-x64` jobs should complete green. If `codesign --verify` or `notarytool submit` fails, check the job log for the specific secret that was rejected."
8. `## Windows SmartScreen Note` section (per D-04). Single short paragraph stating verbatim: "The Windows MSI installer published by the release workflow is **unsigned** in v0.4.0. End users will see a Microsoft Defender SmartScreen warning when running the installer; they can bypass it via *More info → Run anyway*. Authenticode code signing is deferred to a future release because it requires an EV (Extended Validation) code-signing certificate (~$200–500/yr)."

Use plain Markdown — no `> [!NOTE]` admonition syntax (README.md uses bold-prefixed prose notes, see lines 44–49). Every fenced code block must have a language tag (`bash`, `yaml`, or `text`). One blank line before and after every fenced block.

All 7 secret names MUST appear in the document (most appear inside the verbatim block; ensure they also appear in the prose Setup Steps where referenced).
  </action>
  <verify>
    <automated>test -f docs/SIGNING.md && grep -q "MACOS_CERTIFICATE" docs/SIGNING.md && grep -q "MACOS_CERTIFICATE_PASSWORD" docs/SIGNING.md && grep -q "MACOS_SIGNING_IDENTITY" docs/SIGNING.md && grep -q "MACOS_KEYCHAIN_PASSWORD" docs/SIGNING.md && grep -q "APPLE_ID" docs/SIGNING.md && grep -q "APPLE_TEAM_ID" docs/SIGNING.md && grep -q "APPLE_APP_SPECIFIC_PASSWORD" docs/SIGNING.md && grep -q "Apple Developer Program" docs/SIGNING.md && grep -q '\$99' docs/SIGNING.md && grep -q "SmartScreen" docs/SIGNING.md && grep -q "v0.4.0-test1" docs/SIGNING.md && grep -q "base64 -i" docs/SIGNING.md && grep -q "openssl rand -hex 16" docs/SIGNING.md && grep -q "macOS Signing & Notarization" docs/SIGNING.md && grep -q "Acme Corp" docs/SIGNING.md && grep -q "Developer ID Application" docs/SIGNING.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/SIGNING.md` exists
    - `grep -c "^# macOS Signing & Notarization" docs/SIGNING.md` returns `1` (exactly one H1)
    - `grep -c "^## " docs/SIGNING.md` returns `5` (Overview, Setup Steps, Secrets Reference, Verify Your Setup, Windows SmartScreen Note)
    - All 7 secret names present: `MACOS_CERTIFICATE`, `MACOS_CERTIFICATE_PASSWORD`, `MACOS_SIGNING_IDENTITY`, `MACOS_KEYCHAIN_PASSWORD`, `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD` — verifiable via `grep -c "MACOS_CERTIFICATE\|MACOS_CERTIFICATE_PASSWORD\|MACOS_SIGNING_IDENTITY\|MACOS_KEYCHAIN_PASSWORD\|APPLE_ID\|APPLE_TEAM_ID\|APPLE_APP_SPECIFIC_PASSWORD" docs/SIGNING.md` returns ≥ 7
    - `grep -q 'Apple Developer Program' docs/SIGNING.md` and `grep -q '\$99' docs/SIGNING.md` (Apple Developer Program + cost stated)
    - `grep -q 'SmartScreen' docs/SIGNING.md` and `grep -q 'EV' docs/SIGNING.md` (SmartScreen note with EV cert mention)
    - `grep -q 'v0.4.0-test1' docs/SIGNING.md` (verify tag example present)
    - `grep -q 'base64 -i' docs/SIGNING.md` and `grep -q 'openssl rand -hex 16' docs/SIGNING.md` (CLI commands present)
    - `grep -q '\-\-mac-signing-key-user-name' docs/SIGNING.md` (gotcha explanation references jpackage flag)
    - Document contains the verbatim secrets block: `grep -q '── Phase 21: macOS Signing' docs/SIGNING.md` AND `grep -q '── Phase 22: macOS Notarization' docs/SIGNING.md`
    - At least one fenced ```yaml block: `grep -c '^\`\`\`yaml' docs/SIGNING.md` returns ≥ 1
    - At least three fenced ```bash blocks: `grep -c '^\`\`\`bash' docs/SIGNING.md` returns ≥ 3
    - No real secret values committed (defense): `grep -E '[A-Za-z0-9+/]{40,}={0,2}' docs/SIGNING.md` returns no matches OUTSIDE comment placeholders (manual scan acceptable; the verbatim block contains only descriptions, no actual base64 payloads)
  </acceptance_criteria>
  <done>
docs/SIGNING.md exists at repository root under `docs/` and contains: H1 title, Apple Developer Program prerequisite with $99/yr, Overview, 7 numbered Setup Steps, verbatim secrets reference block from release.yml lines 1–41, MACOS_SIGNING_IDENTITY name-portion gotcha, Verify section with `v0.4.0-test1` tag example, Windows SmartScreen note with EV cert deferral. All 7 secret names present. No actual secret values — only placeholders and CLI generation commands.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Contributing / Release Setup section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — exact insertion point is between `## Development Notes` and `## License`, currently lines 81–91)
    - .planning/phases/23-signing-documentation/23-PATTERNS.md (README.md analog section)
    - .planning/phases/23-signing-documentation/23-CONTEXT.md (decision D-05)
  </read_first>
  <action>
Edit `README.md`. Insert a new section immediately after the existing `## Development Notes` section and immediately before the existing `## License` section.

The new section content MUST be exactly (per D-05 and 23-PATTERNS.md analog):

```markdown
## Contributing / Release Setup

To configure macOS signing and notarization secrets for CI releases, see [`docs/SIGNING.md`](docs/SIGNING.md).
```

Preserve a single blank line before and after the new section. Do NOT modify any other section. Do NOT change `## License` content. Do NOT bump the version line. Do NOT modify the screenshot, prerequisites, build/run, tech stack, project structure, or development notes sections.
  </action>
  <verify>
    <automated>grep -q "^## Contributing / Release Setup$" README.md && grep -q '\[`docs/SIGNING.md`\](docs/SIGNING.md)' README.md && grep -q "^## License$" README.md && awk '/^## Contributing \/ Release Setup$/{c=NR} /^## License$/{l=NR} END{exit (c>0 && l>c) ? 0 : 1}' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Contributing / Release Setup$' README.md` returns `1`
    - `grep -q '\[`docs/SIGNING.md`\](docs/SIGNING.md)' README.md` (exact backtick-wrapped relative link present)
    - The Contributing / Release Setup section appears BEFORE `## License` (verified by awk one-liner in `<verify>`)
    - `grep -c '^## License$' README.md` still returns `1` (License section preserved)
    - `grep -c '^## Development Notes$' README.md` still returns `1` (Development Notes section preserved)
    - `grep -q '^**Version:** 0.3.0$' README.md` — version line untouched
    - `grep -c '^# XSLEditor$' README.md` returns `1` (H1 untouched)
    - Total `## ` H2 count increased by exactly 1 from the original (was 9, must now be 10): `grep -c '^## ' README.md` returns `10`
  </acceptance_criteria>
  <done>
README.md contains a new `## Contributing / Release Setup` section between `## Development Notes` and `## License` with a single-sentence pointer to `docs/SIGNING.md` formatted as a backtick-wrapped relative markdown link. All other README content is byte-identical to the prior version.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add docs/SIGNING.md pointer comment to release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (lines 1-50 — see existing banner block ending at line 41 and `name: Release` at line 43)
    - .planning/phases/23-signing-documentation/23-PATTERNS.md (release.yml pointer comment pattern)
    - .planning/phases/23-signing-documentation/23-CONTEXT.md (Integration Points)
  </read_first>
  <action>
Edit `.github/workflows/release.yml`. Insert exactly one line after the existing closing banner line `# =============================================================================` (currently line 41) and before the blank line that precedes `name: Release` (currently line 43).

The inserted line MUST be exactly:

```
# Full setup guide: docs/SIGNING.md
```

(plain `#` comment, no banner decoration, no leading/trailing whitespace beyond the `#`). Preserve exactly one blank line between this new comment line and `name: Release`. Do NOT modify the banner block content above. Do NOT modify any job, step, or workflow logic. Do NOT modify the `on:` trigger.
  </action>
  <verify>
    <automated>grep -q "^# Full setup guide: docs/SIGNING.md$" .github/workflows/release.yml && grep -q "^name: Release$" .github/workflows/release.yml && awk '/^# Full setup guide: docs\/SIGNING.md$/{p=NR} /^name: Release$/{n=NR} END{exit (p>0 && n>p && n-p<=3) ? 0 : 1}' .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^# Full setup guide: docs/SIGNING.md$' .github/workflows/release.yml` returns `1`
    - The pointer line appears AFTER the closing `# ====` banner line and BEFORE `name: Release` (gap ≤ 3 lines, verified by awk in `<verify>`)
    - `grep -c '^name: Release$' .github/workflows/release.yml` still returns `1` (workflow name preserved)
    - The original 7 secret names still present in the header block: `grep -c "MACOS_CERTIFICATE\|MACOS_CERTIFICATE_PASSWORD\|MACOS_SIGNING_IDENTITY\|MACOS_KEYCHAIN_PASSWORD\|APPLE_ID\|APPLE_TEAM_ID\|APPLE_APP_SPECIFIC_PASSWORD" .github/workflows/release.yml` returns ≥ 7
    - Workflow YAML still parses (sanity): `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0 (or skip if no python3 available — the awk gate is sufficient)
  </acceptance_criteria>
  <done>
.github/workflows/release.yml contains the single pointer line `# Full setup guide: docs/SIGNING.md` between the closing `# ====` banner line and `name: Release`. No other workflow content changed. The file remains valid YAML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Public GitHub | All committed files are publicly visible on the GitHub remote |
| Documentation → Reader | Reader follows CLI commands from the doc against their own local environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | docs/SIGNING.md | mitigate | Doc MUST contain only placeholder values, generation commands (`openssl rand -hex 16`, `base64 -i path/to/cert.p12`), and example identifiers (`Acme Corp`, `ABC123XYZ7`). NO actual base64 payloads, NO real Apple ID, NO real team ID, NO real app-specific password. Acceptance criteria includes a defensive grep against long base64-shaped strings. |
| T-23-02 | Information Disclosure | README.md / release.yml pointer comments | mitigate | Pointer comments are plain references to a doc path — no secrets, no credentials. Verified by acceptance criteria limiting added content to a single sentence (README) and a single comment line (release.yml). |
| T-23-03 | Tampering | release.yml | accept | Edit is a single comment line; cannot affect workflow logic. Acceptance criteria asserts only the pointer line is added, banner content and `name:`/`on:` blocks are preserved. |
| T-23-04 | Spoofing | docs/SIGNING.md (instructions to readers) | mitigate | Doc references only official Apple URLs (`https://developer.apple.com/account`, `https://appleid.apple.com`, `https://developer.apple.com/programs/`). Reviewers must confirm no third-party intermediary URLs are introduced. |
| T-23-05 | Repudiation | git history | accept | Standard git commit attribution applies; no special audit logging required for documentation changes. |
</threat_model>

<verification>
After all 3 tasks complete, run from repository root:

```bash
# Files exist and contain required content
test -f docs/SIGNING.md && \
test -f README.md && \
test -f .github/workflows/release.yml

# All 7 secrets named in SIGNING.md
for s in MACOS_CERTIFICATE MACOS_CERTIFICATE_PASSWORD MACOS_SIGNING_IDENTITY MACOS_KEYCHAIN_PASSWORD APPLE_ID APPLE_TEAM_ID APPLE_APP_SPECIFIC_PASSWORD; do
  grep -q "$s" docs/SIGNING.md || { echo "MISSING: $s"; exit 1; }
done

# Cross-links in place
grep -q '\[`docs/SIGNING.md`\](docs/SIGNING.md)' README.md
grep -q '^# Full setup guide: docs/SIGNING.md$' .github/workflows/release.yml

# No accidental secret leakage (defensive: long base64-shaped strings)
! grep -E '^[A-Za-z0-9+/]{60,}={0,2}$' docs/SIGNING.md

# Workflow YAML still valid
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" 2>/dev/null || echo "WARN: python3/PyYAML unavailable; rely on awk gate from Task 3"
```
</verification>

<success_criteria>
- All 3 ROADMAP §Phase 23 success criteria satisfied:
  1. `docs/SIGNING.md` exists and documents end-to-end process: export `.p12` from Keychain Access, base64-encode it, set all 7 secrets in GitHub Actions
  2. Doc names each secret and explains what it expects (via verbatim secrets reference block + Setup Steps prose)
  3. Doc notes Apple Developer Program $99/yr requirement and Windows SmartScreen + unsigned MSI explanation
- README.md is discoverable: a new contributor browsing the README sees the link without opening `docs/`
- release.yml header points to the canonical guide
- SIGN-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/23-signing-documentation/23-01-SUMMARY.md` summarizing:
- Files created: `docs/SIGNING.md`
- Files modified: `README.md`, `.github/workflows/release.yml`
- Secret names covered: list all 7
- Verbatim block source: `release.yml` lines 1–41
- Verification: all acceptance criteria gates passed
</output>
