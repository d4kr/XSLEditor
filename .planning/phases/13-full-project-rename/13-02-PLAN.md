---
phase: 13-full-project-rename
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: [
  "build.gradle",
  "settings.gradle",
  "src/main/resources/ch/ti/gagi/xsleditor/ui/main.fxml",
  "src/main/resources/ch/ti/gagi/xsleditor/ui/main.css",
  "src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java",
  "src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java",
  "src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java"
]
autonomous: true
requirements: [RENAME-08, RENAME-01]

must_haves:
  truths:
    - "Build scripts reference XSLEditorApp in the new package"
    - "FXML controller points to ch.ti.gagi.xsleditor.ui.MainController"
    - "APP_NAME constant is updated to 'XSLEditor'"
    - "UI strings like 'About XSLEditor' are updated to 'About XSLEditor'"
  artifacts:
    - path: "build.gradle"
      provides: "Updated mainClass and manifest attributes"
    - path: "src/main/resources/ch/ti/gagi/xsleditor/ui/main.fxml"
      provides: "Updated controller and menu item text"
  key_links:
    - from: "build.gradle"
      to: "ch.ti.gagi.xsleditor.XSLEditorApp"
      via: "mainClass configuration"
---

<objective>
Update the build system, UI resources (FXML/CSS), and application constants to reflect the new project name.

Purpose: Ensure the application can be built and run under the new name with consistent UI strings.
Output: Updated build.gradle, settings.gradle, FXML resources, and Java UI constants.
</objective>

<execution_context>
@~/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-full-project-rename/13-01-SUMMARY.md
@.planning/phases/13-full-project-rename/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update build and settings files</name>
  <files>build.gradle, settings.gradle</files>
  <action>
    1. In build.gradle, update mainClass from 'ch.ti.gagi.xsleditor.XSLEditorApp' to 'ch.ti.gagi.xsleditor.XSLEditorApp'.
    2. In build.gradle, update 'Main-Class' attribute in jar and shadowJar tasks.
    3. In settings.gradle, verify rootProject.name is 'xsleditor' (already correct but ensure consistency).
    4. Per RENAME-08.
  </action>
  <verify>
    <automated>grep "ch.ti.gagi.xsleditor.XSLEditorApp" build.gradle</automated>
  </verify>
  <done>Build configuration updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update FXML and CSS resources</name>
  <files>src/main/resources/ch/ti/gagi/xsleditor/ui/main.fxml, src/main/resources/ch/ti/gagi/xsleditor/ui/main.css</files>
  <action>
    1. In main.fxml, update fx:controller to 'ch.ti.gagi.xsleditor.ui.MainController'.
    2. In main.fxml, update "About XSLEditor..." menu item text to "About XSLEditor...".
    3. In main.css, update comments referencing XSLEditor to XSLEditor.
  </action>
  <verify>
    <automated>grep "ch.ti.gagi.xsleditor.ui.MainController" src/main/resources/ch/ti/gagi/xsleditor/ui/main.fxml</automated>
  </verify>
  <done>FXML and CSS files updated.</done>
</task>

<task type="auto">
  <name>Task 3: Update application constants and UI strings in Java code</name>
  <files>src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java, src/main/java/ch/ti/gagi/xsleditor/ui/AboutDialog.java, src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java</files>
  <action>
    1. In XSLEditorApp.java, change APP_NAME from "XSLEditor" to "XSLEditor".
    2. In AboutDialog.java, update strings "XSLEditor v" to "XSLEditor v".
    3. In MainController.java, update Javadoc or comments referencing "XSLEditor" (case-sensitive) to "XSLEditor".
    4. Per RENAME-01.
  </action>
  <verify>
    <automated>grep "XSLEditor" src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java | grep "APP_NAME"</automated>
  </verify>
  <done>Java constants and UI strings updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Availability | FXML Loader | mitigate | Verify application starts without FXML loading errors (handled in verification step). |
</threat_model>

<verification>
Check build.gradle and main.fxml for correct package/class references.
</verification>

<success_criteria>
- build.gradle points to the new main class.
- FXML controller is correctly linked to the new package.
- Application displays "XSLEditor" in the title bar and About dialog.
</success_criteria>

<output>
After completion, create .planning/phases/13-full-project-rename/13-02-SUMMARY.md
</output>
