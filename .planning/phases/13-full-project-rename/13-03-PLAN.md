---
phase: 13-full-project-rename
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified: [
  "README.md",
  "CLAUDE.md",
  ".planning/**/*.md",
  "ROADMAP.md"
]
autonomous: true
requirements: [RENAME-02, RENAME-03]

must_haves:
  truths:
    - "README.md and CLAUDE.md use the name XSLEditor"
    - "All planning documents in .planning/ use the name XSLEditor"
    - "Project builds and tests pass after rename"
    - "Application starts with the correct name"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation"
    - path: "ROADMAP.md"
      provides: "Updated project roadmap"
---

<objective>
Update all project documentation and planning files with the new name and perform final verification.

Purpose: Consistency across all project artifacts and verification of build integrity.
Output: Updated markdown files and a successful build/test cycle.
</objective>

<execution_context>
@/Users/kraehend/Developer/XLSEditor/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-full-project-rename/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update documentation and planning files</name>
  <files>README.md, CLAUDE.md, ROADMAP.md, .planning/**/*.md</files>
  <action>
    1. Perform a global case-sensitive search and replace: 'XLSEditor' -> 'XSLEditor'.
    2. Perform a global case-sensitive search and replace: 'xlseditor' -> 'xsleditor'.
    3. Update the roadmap phase goal and requirement status if needed.
    4. Per RENAME-02, RENAME-03.
  </action>
  <verify>
    <automated>grep -r "XLSEditor" . | grep -v ".git" | grep -v "RENAME-01" | wc -l | grep "^0$"</automated>
  </verify>
  <done>All documentation updated.</done>
</task>

<task type="auto">
  <name>Task 2: Final build and test verification</name>
  <files>N/A</files>
  <action>
    1. Run './gradlew clean test' to ensure everything compiles and tests pass in the new structure.
    2. Run './gradlew run' and visually verify the application title is "XSLEditor".
  </action>
  <verify>
    <automated>./gradlew clean test</automated>
  </verify>
  <done>Build and tests verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Integrity | Documentation | accept | Manual check of README for broken links/references after rename. |
</threat_model>

<verification>
Run grep across the whole project for the old name.
</verification>

<success_criteria>
- No occurrences of 'XLSEditor' (case-sensitive) or 'xlseditor' (case-sensitive) remain in the codebase.
- './gradlew test' passes.
- './gradlew run' launches the app successfully.
</success_criteria>

<output>
After completion, create .planning/phases/13-full-project-rename/13-03-SUMMARY.md
</output>
