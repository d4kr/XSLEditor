---
phase: 13-full-project-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java",
  "src/main/java/ch/ti/gagi/xsleditor/**/*.java",
  "src/test/java/ch/ti/gagi/xsleditor/**/*.java"
]
autonomous: true
requirements: [RENAME-04, RENAME-05, RENAME-06, RENAME-07]

must_haves:
  truths:
    - "All Java source files are moved to ch.ti.gagi.xsleditor package directory"
    - "All Java files have 'package ch.ti.gagi.xsleditor' declaration"
    - "All imports correctly reference the new package"
    - "XSLEditorApp.java is renamed to XSLEditorApp.java"
  artifacts:
    - path: "src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java"
      provides: "Main application entry point with new name"
    - path: "src/main/java/ch/ti/gagi/xsleditor/ui/MainController.java"
      provides: "Updated controller in new package"
  key_links:
    - from: "src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java"
      to: "ch.ti.gagi.xsleditor.ui"
      via: "import statements"
---

<objective>
Refactor the Java package structure and rename the main application class. This is the first step of the project rename, focusing on the core source code structure.

Purpose: Align the codebase structure with the new project name "XSLEditor".
Output: Moved directories and updated package/import declarations in all 50+ Java files.
</objective>

<execution_context>
@/Users/kraehend/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-full-project-rename/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package directories and main class file</name>
  <files>src/main/java/ch/ti/gagi/xsleditor, src/test/java/ch/ti/gagi/xsleditor, src/main/resources/ch/ti/gagi/xsleditor</files>
  <action>
    1. Rename src/main/java/ch/ti/gagi/xsleditor to src/main/java/ch/ti/gagi/xsleditor
    2. Rename src/test/java/ch/ti/gagi/xsleditor to src/test/java/ch/ti/gagi/xsleditor
    3. Rename src/main/resources/ch/ti/gagi/xsleditor to src/main/resources/ch/ti/gagi/xsleditor
    4. Rename src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java to src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java
    5. Per RENAME-04, RENAME-05.
  </action>
  <verify>
    <automated>ls -d src/main/java/ch/ti/gagi/xsleditor && ls src/main/java/ch/ti/gagi/xsleditor/XSLEditorApp.java</automated>
  </verify>
  <done>Directories renamed and main class file renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Update package declarations and imports in all Java files</name>
  <files>src/**/*.java</files>
  <action>
    1. Search and replace 'package ch.ti.gagi.xsleditor' with 'package ch.ti.gagi.xsleditor' in all .java files.
    2. Search and replace 'import ch.ti.gagi.xsleditor' with 'import ch.ti.gagi.xsleditor' in all .java files.
    3. Update the class definition in XSLEditorApp.java from 'public class XSLEditorApp' to 'public class XSLEditorApp'.
    4. Per RENAME-06, RENAME-07.
  </action>
  <verify>
    <automated>grep -r "ch.ti.gagi.xsleditor" src | wc -l | grep "^0$"</automated>
  </verify>
  <done>All Java files updated with new package and class names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A - Internal refactoring.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Integrity | Build System | mitigate | Ensure ./gradlew clean is run before testing to avoid stale class conflicts. |
</threat_model>

<verification>
Check if directories exist and grep for old package names.
</verification>

<success_criteria>
- No directories named 'xsleditor' exist under src/main/java or src/test/java.
- No Java file contains the string 'ch.ti.gagi.xsleditor'.
- XSLEditorApp.java contains 'public class XSLEditorApp'.
</success_criteria>

<output>
After completion, create .planning/phases/13-full-project-rename/13-01-SUMMARY.md
</output>
