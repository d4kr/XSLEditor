---
phase: 21-macos-signing
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements:
  - MACOS-03

must_haves:
  truths:
    - "release.yml contains a comment block documenting all 7 required secrets with their expected values and usage"
    - "The 4 Phase-21 secrets (MACOS_CERTIFICATE, MACOS_CERTIFICATE_PASSWORD, MACOS_SIGNING_IDENTITY, MACOS_KEYCHAIN_PASSWORD) are documented as used in the signing steps"
    - "The 3 Phase-22 secrets (APPLE_ID, APPLE_TEAM_ID, APPLE_APP_SPECIFIC_PASSWORD) are documented as reserved for notarization"
    - "ROADMAP.md Phase 21 plan count is updated to 2"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Secrets documentation comment block at top of file"
      contains: "MACOS_CERTIFICATE"
  key_links:
    - from: "release.yml secrets comment block"
      to: "Import signing certificate step (env: MACOS_CERTIFICATE)"
      via: "documentation reference"
      pattern: "MACOS_CERTIFICATE"
---

<objective>
Add a secrets documentation comment block to the top of release.yml listing all 7 required GitHub
Actions secrets with their expected values, how to obtain them, and which CI phase uses each.

Purpose: The workflow reads these secrets but there is no in-file documentation of what values each
expects. A maintainer must be able to understand the full signing secret surface from the workflow
file alone (before reading docs/SIGNING.md in Phase 23).

Output:
- .github/workflows/release.yml (modified: documentation comment block added at top)
- ROADMAP.md (updated: Phase 21 plan count set to 2)
</objective>

<execution_context>
@~/Developer/XSLEditor/.claude/get-shit-done/workflows/execute-plan.md
@~/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@~/Developer/XSLEditor/.planning/ROADMAP.md
@~/Developer/XSLEditor/.planning/REQUIREMENTS.md
@~/Developer/XSLEditor/.planning/phases/21-macos-signing/21-01-SUMMARY.md
@~/Developer/XSLEditor/.planning/phases/21-macos-signing/21-RESEARCH.md

<interfaces>
<!-- Secrets documented in this plan (from RESEARCH.md section "The 7 Required GitHub Secrets"):

Phase 21 secrets (signing — used in this workflow):
  MACOS_CERTIFICATE         — Base64-encoded .p12 export of Developer ID Application certificate
                               Export from Keychain Access; base64 encode: base64 -i cert.p12 | pbcopy
  MACOS_CERTIFICATE_PASSWORD — Password set when exporting the .p12 from Keychain Access
  MACOS_SIGNING_IDENTITY    — Name portion ONLY of the certificate subject
                               e.g. "Acme Corp" NOT "Developer ID Application: Acme Corp (ABC123)"
  MACOS_KEYCHAIN_PASSWORD   — Any random string; password for ephemeral CI keychain
                               Generate: openssl rand -hex 16

Phase 22 secrets (notarization — reserved, not yet used):
  APPLE_ID                  — Apple ID email address of the developer account
  APPLE_TEAM_ID             — 10-character team ID from developer.apple.com/account
  APPLE_APP_SPECIFIC_PASSWORD — App-specific password from appleid.apple.com
                                 Sign-In and Security → App-Specific Passwords → Generate
-->
</interfaces>
</context>

<tasks>

<task type="auto" id="21-02-T1">
  <name>Task 1: Add secrets documentation comment to release.yml</name>
  <files>.github/workflows/release.yml</files>

  <read_first>
    - ~/Developer/XSLEditor/.github/workflows/release.yml
      (read the full file to know exactly where to insert the comment block — insert after the
      `name: Release` line and before the `on:` block, or at the very top before `name:`)
    - ~/Developer/XSLEditor/.planning/phases/21-macos-signing/21-RESEARCH.md
      (section "The 7 Required GitHub Secrets" — source of truth for secret names and values)
  </read_first>

  <action>
Insert a YAML comment block at the very top of .github/workflows/release.yml (before the
`name: Release` line). The comment block must document all 7 required secrets.

Insert the following block as lines 1–42 of the file (pushing the existing `name: Release`
line to line 43):

# =============================================================================
# Required GitHub Actions Secrets
# =============================================================================
# Configure these secrets at:
# GitHub → Settings → Secrets and variables → Actions → New repository secret
#
# ── Phase 21: macOS Signing (used in package-macos-arm and package-macos-x64) ──
#
# MACOS_CERTIFICATE
#   Base64-encoded Developer ID Application certificate (.p12 export from Keychain Access)
#   How to obtain: Keychain Access → right-click "Developer ID Application: Name (TEAM)"
#                  → Export → save as .p12 with a password
#   How to encode: base64 -i /path/to/cert.p12 | pbcopy
#
# MACOS_CERTIFICATE_PASSWORD
#   The password you set when exporting the .p12 from Keychain Access.
#
# MACOS_SIGNING_IDENTITY
#   The NAME PORTION ONLY of the certificate subject.
#   Correct:   Acme Corp
#   Incorrect: Developer ID Application: Acme Corp (ABC123XYZ7)
#   jpackage uses --mac-signing-key-user-name which expects the name only.
#
# MACOS_KEYCHAIN_PASSWORD
#   Any random string used as the password for the ephemeral CI keychain.
#   Generate: openssl rand -hex 16
#   This keychain is created and deleted on every CI run.
#
# ── Phase 22: macOS Notarization (reserved — not yet used in this workflow) ──
#
# APPLE_ID
#   Apple ID email address of the developer account that owns the certificate.
#
# APPLE_TEAM_ID
#   10-character team identifier from developer.apple.com/account (e.g. ABC123XYZ7)
#
# APPLE_APP_SPECIFIC_PASSWORD
#   App-specific password for the Apple ID.
#   Generate: appleid.apple.com → Sign-In and Security → App-Specific Passwords
#
# =============================================================================

The comment block uses `#` YAML comment syntax. It must be inserted as plain text lines at the
very top of the file — GitHub Actions ignores comment lines; this does not affect workflow
execution.

Do NOT modify any other content in the file.
  </action>

  <verify>
    <automated>head -5 ~/Developer/XSLEditor/.github/workflows/release.yml | grep "Required GitHub Actions Secrets" && grep -c "MACOS_CERTIFICATE" ~/Developer/XSLEditor/.github/workflows/release.yml && grep -c "APPLE_APP_SPECIFIC_PASSWORD" ~/Developer/XSLEditor/.github/workflows/release.yml && python3 -c "import yaml; yaml.safe_load(open('~/Developer/XSLEditor/.github/workflows/release.yml'))" && echo "YAML valid"</automated>
  </verify>

  <acceptance_criteria>
    - head -5 .github/workflows/release.yml returns a line containing "Required GitHub Actions Secrets"
    - grep "MACOS_CERTIFICATE" .github/workflows/release.yml returns at least 3 matches (comment + 2 env: blocks in signing steps)
    - grep "MACOS_CERTIFICATE_PASSWORD" .github/workflows/release.yml returns at least 3 matches
    - grep "MACOS_SIGNING_IDENTITY" .github/workflows/release.yml returns at least 3 matches
    - grep "MACOS_KEYCHAIN_PASSWORD" .github/workflows/release.yml returns at least 3 matches
    - grep "APPLE_ID" .github/workflows/release.yml returns at least 1 match (comment only)
    - grep "APPLE_TEAM_ID" .github/workflows/release.yml returns at least 1 match (comment only)
    - grep "APPLE_APP_SPECIFIC_PASSWORD" .github/workflows/release.yml returns at least 1 match (comment only)
    - python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" exits 0 (valid YAML)
    - The existing workflow content (name: Release, on:, jobs:) is intact — grep "name: Release" returns 1 match
    - grep "security set-key-partition-list" .github/workflows/release.yml returns 2 matches (signing steps from Plan 01 unchanged)
  </acceptance_criteria>

  <done>
release.yml begins with a comment block documenting all 7 secrets; the file remains valid YAML;
all signing steps from Plan 01 are intact.
  </done>
</task>

<task type="auto" id="21-02-T2">
  <name>Task 2: Update ROADMAP.md Phase 21 plan count</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - ~/Developer/XSLEditor/.planning/ROADMAP.md
      (find the Phase 21 section; update Plans count and plan list)
  </read_first>

  <action>
Update the Phase 21 section in .planning/ROADMAP.md:

1. Set the plan count to 2:
   Change: **Plans**: TBD
   To:     **Plans**: 2 plans

2. Add the plan list after the plan count line:
   Plans:
   - [ ] 21-01-PLAN.md — entitlements.plist + CI signing sequence in both macOS jobs
   - [ ] 21-02-PLAN.md — Secrets documentation comment in release.yml

Do not modify any other section of ROADMAP.md.
  </action>

  <verify>
    <automated>grep -A 5 "Phase 21: macOS Signing" ~/Developer/XSLEditor/.planning/ROADMAP.md | grep "2 plans"</automated>
  </verify>

  <acceptance_criteria>
    - grep "2 plans" .planning/ROADMAP.md returns at least 1 match in the Phase 21 section
    - grep "21-01-PLAN.md" .planning/ROADMAP.md returns 1 match
    - grep "21-02-PLAN.md" .planning/ROADMAP.md returns 1 match
    - The Phase 22 and Phase 23 sections are unchanged (grep "Phase 22" .planning/ROADMAP.md returns the same content as before)
  </acceptance_criteria>

  <done>
ROADMAP.md Phase 21 shows "2 plans" with both plan files listed; all other sections unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML comment → reader | Comment block is plain text; no execution path; information only |
| Secret names in comments | Comments expose secret names (not values); names are already visible in the env: blocks added in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Information Disclosure | Secret names exposed in workflow comments | accept | Comments document only secret NAMES and HOW TO OBTAIN values — never the values themselves. Secret names are already visible in the env: blocks. No PII or key material in comments. ASVS L1 V6.4.1 — keys not hardcoded; accepted. |
| T-21-08 | Tampering | Workflow comment modified to mislead maintainer | accept | Comments are informational only; functional signing steps from Plan 01 are unaffected by comment content. Branch protection prevents unauthorized edits. Risk accepted. |
</threat_model>

<verification>
## Phase 21 Plan 02 Verification

1. Comment block is present at the top of release.yml:
   ```
   head -3 .github/workflows/release.yml
   ```
   Must show lines beginning with `#`.

2. All 7 secrets are documented in the comment block:
   ```
   grep "MACOS_CERTIFICATE\|MACOS_CERTIFICATE_PASSWORD\|MACOS_SIGNING_IDENTITY\|MACOS_KEYCHAIN_PASSWORD\|APPLE_ID\|APPLE_TEAM_ID\|APPLE_APP_SPECIFIC_PASSWORD" .github/workflows/release.yml | grep "^#"
   ```
   Must return 7 lines (one per secret in comment form).

3. Workflow remains valid YAML after comment insertion:
   ```
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"
   ```
   Must exit 0.

4. Signing steps from Plan 01 are intact:
   ```
   grep -c "security set-key-partition-list" .github/workflows/release.yml
   ```
   Must return 2.

5. ROADMAP.md updated:
   ```
   grep "2 plans" .planning/ROADMAP.md
   grep "21-01-PLAN.md" .planning/ROADMAP.md
   grep "21-02-PLAN.md" .planning/ROADMAP.md
   ```
   All must return matches.
</verification>

<success_criteria>
1. release.yml opens with a comment block that names and explains all 7 secrets (4 for Phase 21
   signing, 3 reserved for Phase 22 notarization)
2. The comment block correctly states that MACOS_SIGNING_IDENTITY expects the name portion only
   (not the full "Developer ID Application: Name (TEAM)" string)
3. The workflow file remains valid YAML (python3 yaml.safe_load exits 0)
4. All Phase 21 signing steps from Plan 01 are intact (security set-key-partition-list count = 2)
5. ROADMAP.md Phase 21 section shows "2 plans" with both plan files listed
</success_criteria>

<output>
After completion, create .planning/phases/21-macos-signing/21-02-SUMMARY.md following the
template at @~/Developer/XSLEditor/.claude/get-shit-done/templates/summary.md
</output>
